---
phase: 08-vendor-profile-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/MarketplaceProfileItem.yml
  - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/marketplaceProfileItem.section.yml
  - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/marketplaceProfileItem.expiresAt.yml
  - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/marketplaceProfileItem.status.yml
  - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/marketplaceProfileItem.orgId.yml
  - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/marketplaceProfileItem.data.yml
  - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/enums/marketplaceProfileItem.section.yml
autonomous: true
requirements: [VPR-01, VPR-02, VPR-03, VPR-04, VPR-05, VPR-06]
user_setup: []

must_haves:
  truths:
    - "MarketplaceProfileItem class is defined in YAML with all required fields"
    - "Dataloader successfully imports schema without errors"
    - "PR is submitted to zerobias-org/schema:dev branch"
    - "Class ID is deterministic (UUID v5 from YAML content)"
    - "GQL schema will auto-reload within 15 minutes of merge"
  artifacts:
    - path: "~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/MarketplaceProfileItem.yml"
      provides: "GQL class definition with section, expiresAt, status, orgId, data fields"
      must_contain: "extends:\n  - Object"
    - path: "~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/enums/marketplaceProfileItem.section.yml"
      provides: "Section enum with 6 values (CORPORATE_IDENTITY, ATTESTATION, INSURANCE, REFERENCE, PERSONNEL, FINANCIAL)"
      must_contain: "values:"
    - path: "~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/.dataloader-validated"
      provides: "Dataloader validation marker (< 30 min old)"
  key_links:
    - from: "Class definition"
      to: "Field definitions"
      via: "field: marketplaceProfileItem.fieldName references"
      pattern: "field: marketplaceProfileItem\\."
    - from: "Section field"
      to: "Section enum"
      via: "field: marketplaceProfileItem.section references"
      pattern: "marketplaceProfileItem.section"
    - from: "Schema repo branch"
      to: "upstream/dev"
      via: "feat/marketplace-profile-item branch created from upstream/dev"
      pattern: "upstream/dev"
---

<objective>
Create a GQL schema entity `MarketplaceProfileItem` in the ZeroBias platform with typed metadata fields (section, expiresAt, status, orgId) plus a flexible JSON data blob for section-specific content. Validate with dataloader, commit to fork, and submit PR to zerobias-org/schema:dev.

Purpose: Provides platform GQL schema foundation for Phase 9 (Service Layer) and Phase 10 (UI). Enables flexible credential/certification storage with per-section JSON structures.

Output: Merged PR to zerobias-org/schema:dev with 7 YAML files (1 class definition + 5 field definitions + 1 enum definition). Schema reloads on platform within 15 minutes of merge.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md (current phase status)
@.planning/REQUIREMENTS.md (VPR-01 through VPR-06)
@08-CONTEXT.md (user decisions D-01 through D-10)
@08-RESEARCH.md (standard stack, architecture patterns, pitfalls)

Schema Repo Location: ~/Projects/w3geekery/zerobias-org-forks/schema/
Schema Package Dir: package/w3geekery/smemart/
SCHEMA_CHANGE_PROCESS: ~/.planning/docs/SCHEMA_CHANGE_PROCESS.md (full workflow)

Existing Pattern References (schema repo):
- classes/EngagementVettingItem.yml — Closest pattern (typed metadata + enum discriminator)
- classes/Engagement.yml — Multi-field example
- fields/*.yml — Field definition templates
- enums/*.yml — Enum templates
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MarketplaceProfileItem class and field definitions in schema repo</name>
  <files>
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/MarketplaceProfileItem.yml
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/marketplaceProfileItem.section.yml
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/marketplaceProfileItem.expiresAt.yml
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/marketplaceProfileItem.status.yml
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/marketplaceProfileItem.orgId.yml
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/marketplaceProfileItem.data.yml
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/enums/marketplaceProfileItem.section.yml
  </files>
  <read_first>
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/EngagementVettingItem.yml (pattern reference for class structure)
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/ (at least 2 existing field definitions to verify format)
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/enums/ (at least 1 existing enum to verify format)
  </read_first>
  <action>
    **Setup:** Switch to schema repo and create feature branch from upstream/dev per SCHEMA_CHANGE_PROCESS §1.

    ```bash
    cd ~/Projects/w3geekery/zerobias-org-forks/schema
    git fetch upstream
    git checkout -b feat/marketplace-profile-item upstream/dev
    ```

    **Create 1 Class Definition:** `package/w3geekery/smemart/classes/MarketplaceProfileItem.yml`

    Must contain:
    - description: "Vendor/buyer profile item containing credentials, certifications, references, insurance info, or personnel data. Uses section discriminator + JSON data blob for flexible content."
    - extends: [Object]
    - properties: section, expiresAt, status, orgId, data (5 properties, each with `field: marketplaceProfileItem.fieldName`)
    - viewProperties: Name, Section, Status, Expires At (with jsonata and sort values)
    - NO linkTo statements (per D-09 decision)

    Reference EngagementVettingItem.yml for YAML structure/indent/format. Class name is `MarketplaceProfileItem` (CamelCase) matching GQL class name.

    **Create 5 Field Definitions:** Each file at `package/w3geekery/smemart/fields/marketplaceProfileItem.{fieldName}.yml`

    1. marketplaceProfileItem.section.yml: type: string, displayName: "Section", description: "Profile section type — determines content structure"
    2. marketplaceProfileItem.expiresAt.yml: type: string, displayName: "Expires At", description: "ISO 8601 date when credential expires"
    3. marketplaceProfileItem.status.yml: type: string, displayName: "Status", description: "Item lifecycle status (active, expired, draft, archived)"
    4. marketplaceProfileItem.orgId.yml: type: string, displayName: "Organization ID", description: "UUID of owning organization (scoping via scalar, not link)"
    5. marketplaceProfileItem.data.yml: type: string, displayName: "Data", description: "JSON-serialized section-specific data blob (insurance policy details, cert info, etc.)"

    Reference existing field definitions for YAML structure. No `multi: true` flags (all single-valued). All are simple `type: string`.

    **Create 1 Enum Definition:** `package/w3geekery/smemart/enums/marketplaceProfileItem.section.yml`

    Must contain:
    - description: "Profile section categories"
    - displayName: "Marketplace Profile Section"
    - values array with 6 entries (UPPER_SNAKE_CASE, per D-04):
      - CORPORATE_IDENTITY: "Legal registration, tax ID, DBA, incorporation info"
      - ATTESTATION: "Licenses, certifications, accreditations, professional designations"
      - INSURANCE: "D&O, general liability, professional liability, COI"
      - REFERENCE: "Client/project references, case studies, testimonials"
      - PERSONNEL: "Key personnel profiles, resumes, background checks"
      - FINANCIAL: "Financial statements, bank statements, credit reports"

    Reference existing enum definitions for YAML structure.

    **File naming convention verification:**
    - Class file: CamelCase matching GQL class name (`MarketplaceProfileItem.yml`)
    - Field files: lowerCamelCase.fieldName matching the `field:` value in class definition
    - Enum file: lowerCamelCase.fieldName.yml matching the `field:` value

    **Indent/syntax:** All files use 2-space YAML indent. Copy indent patterns exactly from reference files (EngagementVettingItem.yml, existing fields/, existing enums/).
  </action>
  <verify>
    - File existence: All 7 files exist at correct paths (verify with `ls -la`)
    - YAML syntax: `npm run validate` in schema repo package directory exits 0
    - Class references: Grep class definition file for "extends: [Object]" and all 5 field references (`field: marketplaceProfileItem.*`)
    - Enum values: Grep enum file for all 6 UPPER_SNAKE_CASE values
    - Field definitions: Each field file contains `type: string` and `displayName` value
  </verify>
  <acceptance_criteria>
    - MarketplaceProfileItem.yml contains exactly: description, extends: [Object], properties (5 items), viewProperties (4 items), zero linkTo statements
    - All 5 field definition files exist with type: string and displayName
    - Section enum file contains all 6 values: CORPORATE_IDENTITY, ATTESTATION, INSURANCE, REFERENCE, PERSONNEL, FINANCIAL
    - `npm run validate` in schema package dir exits 0 (syntax validation passes)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Validate schema with dataloader and commit to fork</name>
  <files>
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/.dataloader-validated
  </files>
  <read_first>
    ~/.planning/docs/SCHEMA_CHANGE_PROCESS.md section 3 (dataloader workflow)
    ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/MarketplaceProfileItem.yml (the class file just created)
  </read_first>
  <action>
    **Update dataloader to latest version (optional but recommended):**

    ```bash
    dataloader --version 2>&1 | grep "Dataloader v"
    npm install -g @zerobias-com/platform-dataloader@latest
    ```

    **Verify scratch DB is running:**

    ```bash
    docker ps --filter name=supabase-pg-content-dev
    ```

    If not running: `docker start supabase-pg-content-dev`. If container doesn't exist (first-time setup): `npx @zerobias-org/util-content-dev-schema`

    **Run dataloader against schema package:**

    ```bash
    cd ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart

    export PGUSER=postgres PGPASSWORD=welcome PGHOST=localhost PGPORT=15432 PGDATABASE=content_dev PGSSLMODE=disable

    dataloader --content-dev --skip-pgboss --skip-dynamo -d ./
    ```

    Must exit with code 0 and print "Importer finished successfully". If it fails, check:
    - Field definition file exists for every `field:` reference in class definition
    - Field filenames match exactly (`field: marketplaceProfileItem.section` → `fields/marketplaceProfileItem.section.yml`)
    - YAML indent is 2 spaces (no tabs)
    - Enum file exists and is valid YAML

    **Touch validation marker ONLY AFTER dataloader succeeds:**

    ```bash
    touch ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/.dataloader-validated
    ```

    **Commit to fork (feat/marketplace-profile-item branch):**

    ```bash
    cd ~/Projects/w3geekery/zerobias-org-forks/schema

    git add package/w3geekery/smemart/classes/MarketplaceProfileItem.yml
    git add package/w3geekery/smemart/fields/marketplaceProfileItem.*.yml
    git add package/w3geekery/smemart/enums/marketplaceProfileItem.section.yml

    git commit -m "feat(w3geekery): add MarketplaceProfileItem schema class with section enum and JSON data field

    - Single class with section discriminator (6 enum values)
    - Typed metadata fields: expiresAt, status, orgId
    - JSON data blob for section-specific content
    - No bidirectional links (orgId is scalar)
    - Dataloader validated, ready for Platform GQL schema reload

    Addresses: VPR-01, VPR-02, VPR-03, VPR-04, VPR-05, VPR-06

    Session: claude --resume poc/sme-mart"
    ```

    Commit will trigger the Claude hook which validates:
    - We're in w3geekery fork (not ~/Projects/zb/)
    - Branch is from upstream/dev
    - Dataloader marker exists and is fresh (< 30 min)
    - Field definitions exist for all `field:` properties

    If hook fails, re-run dataloader and re-touch marker. Then retry commit.
  </action>
  <verify>
    - Dataloader exit code: `echo $?` after running dataloader command (must be 0)
    - Dataloader success message: "Importer finished successfully" in output
    - Marker file: `ls -la ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/.dataloader-validated` (must exist and be < 30 min old)
    - Commit succeeded: `git log --oneline -1` shows feat commit with MarketplaceProfileItem message
    - Branch state: `git status` shows "nothing to commit" (all 7 files staged and committed)
  </verify>
  <acceptance_criteria>
    - Dataloader runs to completion with exit code 0
    - "Importer finished successfully" appears in dataloader output
    - .dataloader-validated file exists and is dated within last 30 minutes
    - All 7 YAML files are committed to feat/marketplace-profile-item branch
    - Commit message references all 6 VPR requirements
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Push to w3geekery fork and create cross-fork PR to zerobias-org/schema:dev</name>
  <files>
  </files>
  <read_first>
    ~/.planning/docs/SCHEMA_CHANGE_PROCESS.md sections 5-6 (push and PR workflow)
  </read_first>
  <action>
    **Push feature branch to w3geekery fork:**

    ```bash
    cd ~/Projects/w3geekery/zerobias-org-forks/schema
    git push origin feat/marketplace-profile-item -u
    ```

    Verify push succeeded (no errors).

    **Create cross-fork PR to zerobias-org/schema:dev:**

    ```bash
    gh pr create \
      --repo zerobias-org/schema \
      --base dev \
      --head w3geekery:feat/marketplace-profile-item \
      --title "feat: add MarketplaceProfileItem schema class with section enum and JSON data field" \
      --body "$(cat <<'EOF'
    ## Summary

    Adds a single GQL schema entity `MarketplaceProfileItem` to support vendor/buyer profile credential and certification management.

    **Entity Design:**
    - Single class (not separate per section) with `section` enum discriminator (6 values: corporate_identity, attestation, insurance, reference, personnel, financial)
    - Typed metadata fields: `expiresAt` (ISO 8601 string), `status` (lifecycle state), `orgId` (org ownership scalar)
    - JSON `data` field for section-specific structured content (parsed by service layer in Phase 9)
    - No bidirectional links (Phase 11 adds vetting→profile reference separately)

    **Files Changed:**
    - classes/MarketplaceProfileItem.yml (class definition with 5 properties + viewProperties)
    - enums/marketplaceProfileItem.section.yml (6 section values)
    - fields/marketplaceProfileItem.*.yml (5 field definitions: section, expiresAt, status, orgId, data)

    **Validation:**
    - Dataloader passes with exit code 0
    - YAML syntax validated
    - Pattern follows existing EngagementVettingItem structure

    **Integration:**
    - Class ID is deterministic (UUID v5) — consistent across environments
    - Platform GQL schema will reload within 15 minutes of merge
    - Service layer (Phase 9) consumes via GQL read path + Pipeline write path
    - UI (Phase 10) manages CRUD operations

    Closes requirement VPR-01, VPR-02, VPR-03, VPR-04, VPR-05, VPR-06

    Session: claude --resume poc/sme-mart
    EOF
    )"
    ```

    Note: If `gh` CLI is not available or permission issues occur, manually construct the PR URL and provide to user for manual creation. Typical URL format: `https://github.com/zerobias-org/schema/pull/new/dev...w3geekery:feat/marketplace-profile-item`

    **Verify PR is created:**

    Capture the PR URL from output. Example: `https://github.com/zerobias-org/schema/pull/123`

    PR should show:
    - Base: `zerobias-org/schema` branch `dev`
    - Head: `w3geekery/schema` branch `feat/marketplace-profile-item`
    - Files changed: 7 (1 class, 5 fields, 1 enum)
    - CI checks running
  </action>
  <verify>
    - Git push succeeds: `git push origin feat/marketplace-profile-item -u` (no errors, shows "remote: GitHub" feedback)
    - PR is created: `gh pr view --repo zerobias-org/schema` (shows PR number and status, or direct URL check)
    - PR configuration: Base branch is `dev`, head branch is `w3geekery:feat/marketplace-profile-item`
    - PR files: Shows 7 files changed (MarketplaceProfileItem.yml + 5 field YAMLs + 1 enum YAML)
    - CI checks initiated: "checks" section shows tests running or pending
  </verify>
  <acceptance_criteria>
    - Feature branch is pushed to w3geekery/schema origin (verified with `git branch -r | grep feat/marketplace-profile-item`)
    - PR is created against zerobias-org/schema:dev (URL captured and verifiable)
    - PR shows 7 files changed
    - PR title matches task description
    - PR body includes all 6 VPR requirement IDs
    - CI checks are running (or will run on zerobias-org/schema webhook trigger)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Created and submitted MarketplaceProfileItem GQL schema PR to zerobias-org/schema:dev. Dataloader validated locally. PR awaits zerobias-org maintainer review and merge.</what-built>
  <how-to-verify>
    1. Open the PR URL from Task 3 output (GitHub web)
    2. Verify:
       - Base branch is `dev`
       - Head is `w3geekery:feat/marketplace-profile-item`
       - Files show: 1 class YAML, 5 field YAMLs, 1 enum YAML (7 total)
       - CI checks (dataloader validation, YAML syntax) pass or are running
    3. If CI passes, PR is ready for maintainer review. No action needed from us until merge.
    4. After merge (typically within 1-2 business days):
       - Platform GQL schema reloads within ~15 minutes
       - Phase 9 (Service Layer) can begin
  </how-to-verify>
  <resume-signal>
    Confirm PR is visible at the GitHub URL. Type "approved" if ready to proceed, or describe any blocking issues (CI failures, merge conflicts, etc.).
  </resume-signal>
</task>

</tasks>

<verification>
**Phase-level verification (all tasks complete):**

1. Schema files exist and are valid YAML:
   ```bash
   ls -la ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/MarketplaceProfileItem.yml
   ls -la ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/fields/marketplaceProfileItem.*.yml
   ls -la ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/enums/marketplaceProfileItem.section.yml
   ```

2. Dataloader validation passed locally:
   - .dataloader-validated marker exists and is < 30 minutes old
   - Dataloader output contains "Importer finished successfully"

3. PR is submitted to zerobias-org/schema:dev:
   - `gh pr list --repo zerobias-org/schema` shows the PR
   - PR URL is captured for follow-up

4. Requirements coverage (grep VPR IDs in commit message):
   ```bash
   cd ~/Projects/w3geekery/zerobias-org-forks/schema && git log --oneline -1 | grep -E "VPR-0[1-6]"
   ```

**Phase gate:** All schema YAML files exist, dataloader has validated them, and PR is submitted to zerobias-org/schema:dev. Phase can be marked complete when PR is merged (maintainers handle this; we monitor for schema reload within 15 min).
</verification>

<success_criteria>
**Phase 8 is complete when:**

1. **VPR-01**: `MarketplaceProfileItem` GQL schema entity submitted to `zerobias-org/schema:dev` via PR
   - Verify: PR exists, shows 7 files, base is `dev`

2. **VPR-02**: Entity has `section` discriminator field (enum: 6 values)
   - Verify: `enums/marketplaceProfileItem.section.yml` contains CORPORATE_IDENTITY, ATTESTATION, INSURANCE, REFERENCE, PERSONNEL, FINANCIAL

3. **VPR-03**: Entity has JSON `data` field for section-specific content
   - Verify: `fields/marketplaceProfileItem.data.yml` exists with type: string

4. **VPR-04**: Entity is org-scoped (scalar orgId field)
   - Verify: `fields/marketplaceProfileItem.orgId.yml` exists and class includes it

5. **VPR-05**: No bidirectional links (scalar orgId, no linkTo)
   - Verify: Class definition has zero `linkTo` statements

6. **VPR-06**: Schema passes dataloader verification
   - Verify: `.dataloader-validated` marker exists and dataloader exited 0

**Timeline:**
- Task 1-3: ~30-45 min (schema file creation + dataloader validation + PR submission)
- Task 4 (checkpoint): PR review and merge by zerobias-org maintainers (1-2 business days typically)
- Schema reload: ~15 minutes after PR merge to platform GQL
- Next phase (09-vendor-profile-service) can begin after schema reload confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/08-vendor-profile-schema/08-01-SUMMARY.md` with:
- PR number and URL
- Dataloader validation confirmation
- Requirement traceability (VPR-01 through VPR-06 mapped to YAML files)
- Schema reload confirmation (timestamp when GQL queries start working)
- Handoff notes for Phase 9 (Service Layer)
</output>
