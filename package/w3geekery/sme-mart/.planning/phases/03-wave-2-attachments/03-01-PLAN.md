---
phase: 03-wave-2-attachments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/core/services/notes.service.ts
  - src/app/core/services/notes.service.spec.ts
  - src/app/core/services/org-document.service.ts
  - src/app/core/services/org-document.service.spec.ts
autonomous: true
requirements: [MIG-09, MIG-10, MIG-14, MIG-15]
must_haves:
  truths:
    - "User can create a note and see it immediately in the engagement (optimistic update)"
    - "NotesService reads notes from GraphQL instead of SmeMartDbService"
    - "User can upload and download documents; metadata reads from GraphQL"
    - "All existing unit tests pass with Pipeline+GraphQL mocks"
  artifacts:
    - path: "src/app/core/services/notes.service.ts"
      provides: "Note CRUD via PipelineWriteService + GraphqlReadService"
      exports: ["createNote", "listNotes", "updateNote", "deleteNote"]
    - path: "src/app/core/services/org-document.service.ts"
      provides: "SmeMartDocument metadata via Pipeline+GraphQL"
      exports: ["uploadDocument", "listDocuments", "getDocument"]
    - path: "src/app/core/services/notes.service.spec.ts"
      provides: "Updated tests using Pipeline+GQL mocks"
      contains: "fakePipelineWriteService, fakeGraphqlReadService"
    - path: "src/app/core/services/org-document.service.spec.ts"
      provides: "Updated tests using Pipeline+GQL mocks"
      contains: "fakePipelineWriteService, fakeGraphqlReadService"
  key_links:
    - from: "notes.service.ts"
      to: "pipeline-write.service.ts"
      via: "constructor inject (removed SmeMartDbService)"
      pattern: "private.*pipelineWrite.*inject"
    - from: "notes.service.ts"
      to: "graphql-read.service.ts"
      via: "constructor inject (new)"
      pattern: "private.*graphqlRead.*inject"
    - from: "org-document.service.ts"
      to: "pipeline-write.service.ts"
      via: "constructor inject (removed SmeMartDbService)"
      pattern: "private.*pipelineWrite.*inject"
---

<objective>
Migrate NotesService and OrgDocumentService from Neon-backed SmeMartDbService to AuditgraphDB Pipeline+GraphQL pattern. This is a direct continuation of Phase 2's migration approach: swap service injection, replace field transforms, add optimistic updates, and update all unit tests to use Pipeline+GraphQL mocks instead of SmeMartDbService mocks.

Purpose: Core marketplace attachments system (notes + documents) now writes to AuditgraphDB and reads from GraphQL, enabling tagging, resource linking, and eventual project board/task integration.

Output: Two fully migrated services, all tests green, zero component changes required.
</objective>

<execution_context>
@/Users/cstacer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cstacer/Projects/w3geekery/zerobias-org-forks/app/package/w3geekery/sme-mart/.planning/phases/03-wave-2-attachments/03-RESEARCH.md
@/Users/cstacer/Projects/w3geekery/zerobias-org-forks/app/package/w3geekery/sme-mart/.planning/phases/02-wave-1-migrations/02-01-SUMMARY.md
</execution_context>

<context>
@/Users/cstacer/Projects/w3geekery/zerobias-org-forks/app/package/w3geekery/sme-mart/.planning/STATE.md
@/Users/cstacer/Projects/w3geekery/zerobias-org-forks/app/package/w3geekery/sme-mart/.planning/REQUIREMENTS.md
@/Users/cstacer/Projects/w3geekery/zerobias-org-forks/app/package/w3geekery/sme-mart/src/app/core/field-mappings.ts
@/Users/cstacer/Projects/w3geekery/zerobias-org-forks/app/package/w3geekery/sme-mart/src/app/core/gql-types/note.types.ts
@/Users/cstacer/Projects/w3geekery/zerobias-org-forks/app/package/w3geekery/sme-mart/src/app/core/gql-types/document.types.ts
@/Users/cstacer/Projects/w3geekery/zerobias-org-forks/app/package/w3geekery/sme-mart/src/app/test-helpers/angular.ts
@/Users/cstacer/Projects/w3geekery/zerobias-org-forks/app/package/w3geekery/sme-mart/src/app/test-helpers/gql-fixtures.ts

Phase 2 established the migration pattern: inject PipelineWriteService + GraphqlReadService, use field mappings to transform camelCase GQL ↔ snake_case Neon types, optimistic return before fire-and-forget Pipeline push, update all mocks in unit tests.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate NotesService from SmeMartDbService to Pipeline+GraphQL</name>
  <read_first>
    - src/app/core/services/notes.service.ts (current Neon-backed implementation)
    - src/app/core/services/engagements.service.ts (Phase 2 reference: completed migration pattern)
    - src/app/core/field-mappings.ts (NOTE_FIELD_MAPPING constant for transforms)
    - src/app/core/gql-types/note.types.ts (GqlNoteResponse interface)
    - src/app/test-helpers/angular.ts (fakePipelineWriteService, fakeGraphqlReadService)
  </read_first>
  <files>
    - src/app/core/services/notes.service.ts
    - src/app/core/services/notes.service.spec.ts
  </files>
  <action>
    1. Update NotesService constructor: Remove SmeMartDbService inject, add PipelineWriteService and GraphqlReadService injects (follow engagements.service pattern exactly)

    2. Replace createNote() implementation:
       - Build gqlData object with camelCase field names (id, engagementId, title, body, folderId, authorZerobiasUserId, accessLevel, archived, createdAt, updatedAt)
       - Use crypto.randomUUID() for id; new Date().toISOString() for timestamps
       - Map gqlData to Neon type using mapGqlToNeon<Note>(gqlData, NOTE_FIELD_MAPPING.gqlToNeon)
       - Fire-and-forget pipelineWrite.pushEntity('Note', gqlData).catch(err => console.error(...))
       - Return optimistically immediately

    3. Replace listNotes() implementation:
       - Query GraphQL with filters: { engagementId: `.eq.${engagementId}`, archived: '.eq.false' } (RFC4515 syntax)
       - Use graphqlRead.query<GqlNoteResponse>('Note', [...fields...], { filters, pageNumber, pageSize })
       - Map each GQL response to Neon type using NOTE_FIELD_MAPPING.gqlToNeon
       - Return PagedResults.fromArray(items, pageNumber, pageSize, totalCount)

    4. Replace updateNote() implementation (if exists):
       - Similar pattern: build gqlData with updated fields, optimistic return, fire-and-forget pipeline push

    5. Replace deleteNote() implementation (if exists):
       - Same pattern: optimistic update (set archived: true), push to pipeline

    6. Update all unit tests in notes.service.spec.ts:
       - Replace SmeMartDbService mocks with fakePipelineWriteService() and fakeGraphqlReadService()
       - Verify createNote test: pipelineWrite.pushEntity called with 'Note' class and camelCase data
       - Verify listNotes test: graphqlRead.query called with correct filters and field selections
       - All 3-5 note tests must pass; grep for "SmeMartDbService" should return 0 matches in the file

    7. No component changes needed; public API (method signatures) remains unchanged

    Coverage: Addresses MIG-09 (write via Pipeline), MIG-10 (read via GraphQL)
  </action>
  <verify>
    <automated>npm test -- src/app/core/services/notes.service.spec.ts --run 2>&1 | grep -E "(PASS|FAIL|✓|✗)"</automated>
    <automated>grep -n "SmeMartDbService" src/app/core/services/notes.service.ts && echo "ERROR: SmeMartDbService still present" || echo "OK: SmeMartDbService removed"</automated>
    <automated>grep -n "PipelineWriteService\|GraphqlReadService" src/app/core/services/notes.service.ts | wc -l | grep -E "[2-9]|[1-9][0-9]" && echo "OK: new services injected" || echo "ERROR: missing injects"</automated>
  </verify>
  <acceptance_criteria>
    - NotesService no longer imports or injects SmeMartDbService
    - createNote() returns optimistically before Pipeline push completes
    - listNotes() queries GraphQL with RFC4515 filters on engagementId and archived fields
    - All note unit tests pass (expect: 3-5 tests green, 0 failures)
    - Field transforms use NOTE_FIELD_MAPPING.gqlToNeon for all GQL→Neon mappings
    - Zero component imports/usages of NotesService changed (tests focus on internal swap)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate OrgDocumentService metadata from SmeMartDbService to Pipeline+GraphQL</name>
  <read_first>
    - src/app/core/services/org-document.service.ts (current Neon-backed implementation)
    - src/app/core/services/engagements.service.ts (Phase 2 reference: completed migration pattern)
    - src/app/core/field-mappings.ts (DOCUMENT_FIELD_MAPPING constant)
    - src/app/core/gql-types/document.types.ts (GqlDocumentResponse interface)
    - src/app/test-helpers/gql-fixtures.ts (document fixture data)
  </read_first>
  <files>
    - src/app/core/services/org-document.service.ts
    - src/app/core/services/org-document.service.spec.ts
  </files>
  <action>
    1. Update OrgDocumentService constructor: Remove SmeMartDbService inject, add PipelineWriteService and GraphqlReadService (same pattern as NotesService above)

    2. Replace uploadDocument() implementation:
       - Keep file binary upload to FileService unchanged (this stays separate)
       - After FileService upload completes, build gqlData with camelCase field names: id (UUID), engagementId, zbFileId, zbFileVersionId, filename, mimeType, fileSizeBytes, documentType, displayName, uploadedByZerobiasUserId, createdAt, updatedAt
       - Map gqlData to Neon type using DOCUMENT_FIELD_MAPPING.gqlToNeon
       - Fire-and-forget pipelineWrite.pushEntity('SmeMartDocument', gqlData)
       - Return optimistically (do NOT wait for Pipeline)

    3. Replace listDocuments() implementation:
       - Query GraphQL with filters (e.g., engagementId, documentType, archived)
       - Use graphqlRead.query<GqlDocumentResponse>('SmeMartDocument', [...fields...], { filters, pageNumber, pageSize })
       - Map responses using DOCUMENT_FIELD_MAPPING.gqlToNeon
       - Return PagedResults (same as notes)

    4. Replace getDocument() implementation:
       - Use graphqlRead.getById<GqlDocumentResponse>('SmeMartDocument', id, [...fields...])
       - Map response to Neon type
       - Return single document or null

    5. Replace deleteDocument() if exists (soft-delete: archived: true):
       - Optimistic update, fire-and-forget pipeline push (same pattern)

    6. Update all unit tests in org-document.service.spec.ts:
       - Replace SmeMartDbService mocks with fakePipelineWriteService() and fakeGraphqlReadService()
       - Verify uploadDocument test: pipelineWrite.pushEntity called AFTER FileService upload completes
       - Verify listDocuments/getDocument tests: graphqlRead.query/getById called with correct filters
       - All document tests must pass; grep for "SmeMartDbService" should return 0 matches

    7. Verify FileService integration unchanged (document metadata is separate from binary)

    Coverage: Addresses MIG-14 (write metadata via Pipeline), MIG-15 (read metadata via GraphQL)
  </action>
  <verify>
    <automated>npm test -- src/app/core/services/org-document.service.spec.ts --run 2>&1 | grep -E "(PASS|FAIL|✓|✗)"</automated>
    <automated>grep -n "SmeMartDbService" src/app/core/services/org-document.service.ts && echo "ERROR: SmeMartDbService still present" || echo "OK: SmeMartDbService removed"</automated>
    <automated>grep -n "pipelineWrite.pushEntity.*SmeMartDocument" src/app/core/services/org-document.service.ts && echo "OK: Pipeline push present" || echo "ERROR: Pipeline push missing"</automated>
  </verify>
  <acceptance_criteria>
    - OrgDocumentService no longer imports or injects SmeMartDbService
    - uploadDocument() pushes metadata to Pipeline AFTER FileService upload completes
    - uploadDocument() returns optimistically (does not wait for Pipeline)
    - listDocuments/getDocument query GraphQL with correct filters
    - All document unit tests pass (expect: 3-5 tests green, 0 failures)
    - Field transforms use DOCUMENT_FIELD_MAPPING.gqlToNeon
    - FileService integration unchanged (binary upload stays on FileService, metadata on Pipeline+GQL)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify Notes + Documents migration with full test suite</name>
  <read_first>
    - src/app/core/services/notes.service.spec.ts (updated in Task 1)
    - src/app/core/services/org-document.service.spec.ts (updated in Task 2)
    - src/app/core/services/note.roundtrip.spec.ts (Phase 1 infrastructure test)
    - src/app/core/services/document.roundtrip.spec.ts (Phase 1 infrastructure test)
  </read_first>
  <files>
    - (no new files; verification only)
  </files>
  <action>
    1. Run full unit test suite: `npm test -- --run` (vitest)
    2. Verify all Notes service tests pass (Task 1 deliverable)
    3. Verify all OrgDocumentService tests pass (Task 2 deliverable)
    4. Verify roundtrip tests pass (note.roundtrip.spec.ts, document.roundtrip.spec.ts from Phase 1)
    5. Check coverage: Ensure ≥80% for both services
    6. Verify no SmeMartDbService references remain in notes.service.ts and org-document.service.ts
    7. Verify Pipeline mocks are called for all create/update/delete operations
    8. Verify GraphQL mocks are called for all list/get operations
    9. Log final test count and pass rate
    10. If any failures, revert to previous commit and diagnose (not expected — both services follow proven Phase 2 pattern)
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -20</automated>
    <automated>npm test -- --run 2>&1 | grep -E "Test Files|passed|failed" | head -5</automated>
    <automated>grep -rn "SmeMartDbService" src/app/core/services/notes.service.ts src/app/core/services/org-document.service.ts 2>/dev/null | wc -l | grep "^0$" && echo "OK: No SmeMartDbService references" || echo "FAIL: SmeMartDbService still present"</automated>
  </verify>
  <acceptance_criteria>
    - Full test suite runs without errors (npm test -- --run)
    - All Notes service tests passing (≥3 tests)
    - All OrgDocumentService tests passing (≥3 tests)
    - Roundtrip tests passing (note and document field mappings verified)
    - Test coverage ≥80% for both services
    - Zero SmeMartDbService references in either migrated service
    - No component changes required (services maintain public API compatibility)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Phase 3 verification steps:

1. **Service Injection Swap Complete:**
   - grep -n "pipelineWrite\|graphqlRead" src/app/core/services/{notes,org-document}.service.ts
   - Expected: Both services have 2+ references to each (constructor inject, method calls)

2. **Field Mappings Applied:**
   - grep -n "NOTE_FIELD_MAPPING\|DOCUMENT_FIELD_MAPPING" src/app/core/services/{notes,org-document}.service.ts
   - Expected: At least 1 reference per service for transform logic

3. **SmeMartDbService Removed:**
   - grep -rn "SmeMartDbService" src/app/core/services/notes.service.ts src/app/core/services/org-document.service.ts
   - Expected: 0 matches (confirmation of full removal)

4. **Optimistic Updates Implemented:**
   - grep -n "optimistic\|return.*before\|fire-and-forget" src/app/core/services/notes.service.ts
   - Confirm: createNote/updateNote/deleteNote return before pipelineWrite completes

5. **GraphQL Reads Implemented:**
   - grep -n "graphqlRead.query\|graphqlRead.getById" src/app/core/services/notes.service.ts src/app/core/services/org-document.service.ts
   - Expected: listNotes/getNote/listDocuments/getDocument use graphqlRead, not SmeMartDbService

6. **Unit Tests Updated:**
   - npm test -- --run 2>&1 | grep -E "Test Files|passed"
   - Expected: All tests passing, ≥80% coverage

7. **Requirements Traceability:**
   - MIG-09: NotesService.createNote() via Pipeline ✓ Task 1
   - MIG-10: NotesService.listNotes() via GraphQL ✓ Task 1
   - MIG-14: DocumentService.uploadDocument() metadata via Pipeline ✓ Task 2
   - MIG-15: DocumentService.getDocument() metadata via GraphQL ✓ Task 2
</verification>

<success_criteria>
Phase 3 is complete when:

1. ✓ NotesService fully migrated: SmeMartDbService removed, PipelineWriteService + GraphqlReadService injected, all CRUD operations use new services
2. ✓ OrgDocumentService fully migrated: Metadata operations use Pipeline+GraphQL, FileService integration unchanged
3. ✓ All unit tests pass: notes.service.spec.ts and org-document.service.spec.ts green, roundtrip tests verify field mappings
4. ✓ No component changes: Services maintain public API compatibility (method signatures unchanged)
5. ✓ Zero SmeMartDbService references in both services
6. ✓ Optimistic updates implemented: createNote/uploadDocument return before Pipeline completes
7. ✓ All requirements addressed: MIG-09, MIG-10, MIG-14, MIG-15 marked complete
8. ✓ SUMMARY.md created in phase directory documenting completed work
</success_criteria>

<output>
After successful execution, create `.planning/phases/03-wave-2-attachments/03-01-SUMMARY.md` documenting:
- Services migrated: NotesService, OrgDocumentService
- Requirements completed: MIG-09, MIG-10, MIG-14, MIG-15
- Commits created (follow conventional format with session reference)
- Test results (pass count, coverage %)
- Any blockers or deviations from plan
- Lessons learned for Phase 4 (Wave 3 standalone entities)
</output>
