---
phase: 16-form-builder
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - "~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/FormSubmission.yml"
  - "~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/SmeMartProject.yml"
  - "src/app/core/models/form-builder.model.ts"
  - "src/app/core/models/index.ts"
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "FormSubmission schema class exists in zerobias-org/schema with projectId, bidId, submissionData, status fields"
    - "SmeMartProject schema class has new formConfig JSON field for storing buyer-defined form structure"
    - "Both schema changes pass dataloader validation via npm run verify"
    - "Schema PR is merged to zerobias-org/schema:dev"
    - "Form model interfaces are defined and exported from src/app/core/models/index.ts"
  artifacts:
    - path: "~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/FormSubmission.yml"
      provides: "Schema definition for form submission tracking (vendor responses)"
      contains: "projectId, bidId, submissionData, status, submittedAt, reviewedAt, reviewedBy"
    - path: "~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/SmeMartProject.yml"
      provides: "Updated SmeMartProject with formConfig field"
      contains: "formConfig field of type JSON"
    - path: "src/app/core/models/form-builder.model.ts"
      provides: "TypeScript interfaces for FormBuilderConfig, FormFieldConfig, FormSubmission"
      exports: ["FormBuilderConfig", "FormFieldConfig", "FormSection", "FormSubmission", "FormFieldType"]
  key_links:
    - from: "FormSubmission.yml"
      to: "zerobias-org/schema"
      via: "schema repo PR"
      pattern: "class: FormSubmission"
    - from: "FormSubmission.yml"
      to: "SmeMartProject"
      via: "linkTo: SmeMartProject.id.formSubmissions"
      pattern: "linkTo: SmeMartProject"
    - from: "FormSubmission.yml"
      to: "Bid"
      via: "linkTo: Bid.id.formSubmission"
      pattern: "linkTo: Bid"
    - from: "schema PR merge"
      to: "GQL platform"
      via: "dataloader validation"
      pattern: "npm run verify"
    - from: "formConfig field"
      to: "src/app/core/models/form-builder.model.ts"
      via: "TypeScript type definition"
      pattern: "FormBuilderConfig"

---

<objective>
Create schema foundation for FormSubmission entity and add formConfig field to SmeMartProject, establish model definitions, and validate against zerobias-org/schema standards.

Purpose: Enable subsequent service and UI layers to build on proven GQL entity structure (same Wave 0 pattern used in Phases 14-15).
Output: FormSubmission YAML file merged to zerobias-org/schema:dev, SmeMartProject updated with formConfig field, model interfaces committed to src/app/core/models/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-form-builder/16-CONTEXT.md
@.planning/phases/16-form-builder/16-RESEARCH.md

# Schema repo workflow and patterns
@~/Projects/w3geekery/zerobias-org-forks/schema/README.md
@~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/SmeMartProject.yml (reference for structure and linkage patterns)
@~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/RfpInvitation.yml (reference for recent schema addition from Phase 14)

# Dataloader and schema verification
@.claude/notes/zb-graphql-custom-schema-howto.md (section 6: full step-by-step)
@.claude/notes/zb-dataloader-verification.md

# Model patterns from existing SME Mart
@src/app/core/models/index.ts (existing model barrel, add new exports)
@src/app/core/models/engagement.model.ts (reference for entity structure)
</context>

<interfaces>
<!-- Key types and contracts the executor needs for schema creation. -->

From 16-RESEARCH.md (schema section):

**FormSubmission entity:**
```yaml
class: FormSubmission
fields:
  - projectId: UUID (link to SmeMartProject, required)
  - bidId: UUID (link to Bid, required)
  - submissionData: JSON (Record<string, unknown>, stores fieldId => value map, required)
  - status: enum ['draft', 'submitted', 'revised', 'reviewed'] (default 'draft', required)
  - submittedAt: timestamp (when vendor clicked Submit, optional)
  - reviewedAt: timestamp (when buyer marked reviewed, optional)
  - reviewedBy: UUID (party ID of buyer who marked reviewed, optional)
  - createdAt: timestamp
  - updatedAt: timestamp
links:
  - linkTo: SmeMartProject.id.formSubmissions
  - linkTo: Bid.id.formSubmission (one-to-one, optional)
properties:
  - Indexed on: projectId, bidId, status
```

**SmeMartProject formConfig field:**
```yaml
class: SmeMartProject
new_field:
  - name: formConfig
    type: JSON (string serialized JSON, optional)
    description: "Buyer-defined form structure: FormBuilderConfig serialized as JSON"
    indexed: false
```

From 16-RESEARCH.md (TypeScript model section):

**FormFieldConfig interface:**
- id: string (unique within form)
- type: FormFieldType ('text' | 'textarea' | 'dropdown' | 'number' | 'file' | 'checkbox')
- label: string
- required: boolean
- placeholder?: string
- description?: string
- textValidation?: { minLength?, maxLength?, pattern? ('email' | 'phone' | 'url' | 'custom') }
- dropdownOptions?: { value: string, label: string }[]
- numberValidation?: { min?, max?, step? }
- fileUploadConfig?: { allowedMimeTypes?: string[], maxFileSizeBytes?, maxFiles? }
- sectionId?: string (optional section grouping)

**FormBuilderConfig interface:**
- version: 1 (schema version)
- sections?: FormSection[] (optional grouping)
- fields: FormFieldConfig[] (top-level ungrouped fields)
- lockedAt?: Date (when locked on first submission)

From existing patterns (SmeMartProject, RfpInvitation):

**YAML structure for schema classes:**
```yaml
---
class: ClassName
type: standard
plural: classNamePlural
fields:
  - name: fieldName
    type: string | integer | boolean | date | uuid | enum | json
    required: true | false
    indexed: true | false
    enum:
      - value1
      - value2
description: "Human description"
links:
  - linkTo: OtherClass.id.relationshipName
    reverse: otherClass.relationshipNameReverse
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create FormSubmission schema class YAML</name>
  <read_first>
    - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/SmeMartProject.yml
    - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/RfpInvitation.yml
    - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/Bid.yml
    - .planning/phases/16-form-builder/16-RESEARCH.md
  </read_first>
  <action>
Create ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/FormSubmission.yml with the following structure:

```yaml
---
class: FormSubmission
type: standard
plural: formSubmissions
description: "Vendor form submission responses linked to bid and project"

fields:
  - name: projectId
    type: uuid
    required: true
    indexed: true
    description: "Link to SmeMartProject containing the form definition"

  - name: bidId
    type: uuid
    required: true
    indexed: true
    description: "Link to Bid this submission is attached to"

  - name: submissionData
    type: json
    required: true
    description: "Field responses: Record<fieldId, value> for all fields in the form"

  - name: status
    type: enum
    required: true
    indexed: true
    enum:
      - draft
      - submitted
      - revised
      - reviewed
    description: "Submission lifecycle: draft (in-progress), submitted (vendor clicked Submit), revised (edited after submit), reviewed (buyer marked reviewed)"

  - name: submittedAt
    type: date
    required: false
    description: "Timestamp when vendor clicked Submit button (not present for drafts)"

  - name: reviewedAt
    type: date
    required: false
    description: "Timestamp when buyer marked submission as reviewed"

  - name: reviewedBy
    type: uuid
    required: false
    description: "Party ID of buyer who marked reviewed"

links:
  - linkTo: SmeMartProject.id.formSubmissions
    description: "One project has many form submissions"

  - linkTo: Bid.id.formSubmission
    description: "One bid has one form submission (optional)"
```

Commit this file to the schema repo at the path shown in files_modified.
  </action>
  <acceptance_criteria>
  - File ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/FormSubmission.yml exists
  - File contains `class: FormSubmission` and `type: standard`
  - All 8 fields are present (projectId, bidId, submissionData, status, submittedAt, reviewedAt, reviewedBy, plus linkage)
  - status enum contains all four values: draft, submitted, revised, reviewed
  - linkTo references point to SmeMartProject and Bid
  - File is valid YAML (no syntax errors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update SmeMartProject schema to include formConfig field</name>
  <read_first>
    - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/SmeMartProject.yml
    - .planning/phases/16-form-builder/16-RESEARCH.md
  </read_first>
  <action>
Open ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/SmeMartProject.yml and add the following field in the fields section (after requirements field, before documents):

```yaml
  - name: formConfig
    type: json
    required: false
    indexed: false
    description: "Buyer-defined form structure (FormBuilderConfig JSON): defines fields, validation, sections"
```

This field stores the entire FormBuilderConfig object serialized as a JSON string on the project entity. It is optional (not all projects have forms).
  </action>
  <acceptance_criteria>
  - ~/Projects/w3geekery/zerobias-org-forks/schema/package/w3geekery/smemart/classes/SmeMartProject.yml contains formConfig field
  - Field type is json
  - Field required: false
  - Field indexed: false
  - Field description mentions FormBuilderConfig
  - File is valid YAML after addition
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run dataloader validation to verify both schema changes</name>
  <read_first>
    - ~/Projects/w3geekery/zerobias-org-forks/schema/README.md
    - .claude/notes/zb-graphql-custom-schema-howto.md (section 6)
  </read_first>
  <action>
From the schema repo directory (~/Projects/w3geekery/zerobias-org-forks/schema/), run:

```bash
cd ~/Projects/w3geekery/zerobias-org-forks/schema
npm run verify
```

This validates FormSubmission.yml, SmeMartProject.yml updates, and all other classes against the schema spec. It will fail if there are syntax errors, missing required fields, or invalid linkages. The output will include FormSubmission class ID (UUID) — capture this UUID, as it will be needed in Phase 16 Plan 01 for PipelineWriteService class ID mapping.

Expected output includes FormSubmission class ID like: `FormSubmission: {UUID}`. Save this ID to a temporary note.
  </action>
  <acceptance_criteria>
  - `npm run verify` exits with status 0 (no errors)
  - Output shows FormSubmission validated successfully
  - Output shows SmeMartProject validated successfully
  - Output includes FormSubmission class ID (UUID) printed in validation report
  - No dataloader errors about missing fields, invalid types, or malformed YAML
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Create schema PR and merge to zerobias-org/schema:dev</name>
  <read_first>
    - ~/Projects/w3geekery/zerobias-org-forks/schema/README.md
    - .planning/feedback_schema_repo_workflow.md (from MEMORY)
    - .planning/feedback_schema_pr_target.md (from MEMORY)
  </read_first>
  <action>
From the schema repo directory:

1. Create a feature branch: `git checkout -b feat/form-submission-entity`
2. Commit both schema changes: `git add package/w3geekery/smemart/classes/FormSubmission.yml package/w3geekery/smemart/classes/SmeMartProject.yml && git commit -m "feat(sme-mart): add FormSubmission class and formConfig field

- New FormSubmission entity for tracking vendor form responses
- Add formConfig JSON field to SmeMartProject for storing buyer-defined form structure
- Supports draft/submitted/revised/reviewed lifecycle
- Links to SmeMartProject and Bid entities

Verification: npm run verify passes"`
3. Push branch: `git push -u origin feat/form-submission-entity`
4. Create PR against zerobias-org/schema:dev (NOT main) with the commit message
5. Wait for CI check to pass
6. Merge PR to dev branch
7. **IMPORTANT:** After merge, the GQL schema will reload on the platform within ~15 minutes. Note the merge time.

Do NOT proceed to Plan 01 until the schema is live (can verify by querying FormSubmission in GraphQL after the reload window).
  </action>
  <acceptance_criteria>
  - Schema PR created with target branch `dev` (not main)
  - PR title includes "FormSubmission" and "formConfig"
  - CI check passes (green checkmark on PR)
  - PR merged to zerobias-org/schema:dev
  - Merge commit visible in `git log origin/dev`
  - FormSubmission class ID noted (from Task 3 output) for use in Plan 01
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Create TypeScript model interfaces for form builder</name>
  <read_first>
    - src/app/core/models/index.ts
    - src/app/core/models/engagement.model.ts
    - .planning/phases/16-form-builder/16-RESEARCH.md (TypeScript model section)
  </read_first>
  <action>
Create src/app/core/models/form-builder.model.ts with the following interfaces:

```typescript
import { UUID } from '@zerobias-org/types-core-js';

// Field type enumeration
export type FormFieldType = 'text' | 'textarea' | 'dropdown' | 'number' | 'file' | 'checkbox';

// Validation configurations for different field types
export interface TextValidation {
  minLength?: number;
  maxLength?: number;
  pattern?: 'email' | 'phone' | 'url' | 'custom';
  patternValue?: string; // Custom regex if pattern='custom'
}

export interface NumberValidation {
  min?: number;
  max?: number;
  step?: number;
}

export interface FileUploadConfig {
  allowedMimeTypes?: string[]; // e.g., ['application/pdf', 'image/*']
  maxFileSizeBytes?: number;   // e.g., 10 * 1024 * 1024 for 10 MB
  maxFiles?: number;           // Per field (usually 1)
}

export interface DropdownOption {
  value: string;
  label: string;
}

// Single form field configuration
export interface FormFieldConfig {
  id: string;                          // Unique within form (UUID or auto-generated)
  type: FormFieldType;
  label: string;
  required: boolean;
  placeholder?: string;
  description?: string;                // Helper text
  // Type-specific validation
  textValidation?: TextValidation;
  dropdownOptions?: DropdownOption[];
  numberValidation?: NumberValidation;
  fileUploadConfig?: FileUploadConfig;
  sectionId?: string;                  // Optional section grouping
}

// Named section for grouping fields
export interface FormSection {
  id: string;
  label: string;
  fields: FormFieldConfig[];
}

// Complete form builder configuration stored as JSON on SmeMartProject
export interface FormBuilderConfig {
  version: 1;                          // Schema version for future migrations
  sections?: FormSection[];            // Optional grouped sections
  fields: FormFieldConfig[];           // Top-level ungrouped fields
  lockedAt?: Date;                     // Locked on first submission
}

// File reference stored in FormSubmission.submissionData for file fields
export interface FileReference {
  fileId: UUID;
  fileName: string;
  fileSize: number;
}

// Form submission response from vendor
export interface FormSubmission {
  id: UUID;
  projectId: UUID;                     // Link to SmeMartProject
  bidId: UUID;                         // Link to Bid
  submissionData: Record<string, unknown>; // fieldId => value map
  status: 'draft' | 'submitted' | 'revised' | 'reviewed';
  submittedAt?: Date;
  reviewedAt?: Date;
  reviewedBy?: UUID;                   // Party ID of reviewer
  createdAt: Date;
  updatedAt: Date;
}
```

After creating the file, update src/app/core/models/index.ts to add these exports:

```typescript
export * from './form-builder.model';
```

The exact line to add depends on the current structure of index.ts, but it should be in alphabetical order with other export statements.
  </action>
  <acceptance_criteria>
  - File src/app/core/models/form-builder.model.ts exists
  - All 10 interfaces are defined: FormFieldType, TextValidation, NumberValidation, FileUploadConfig, DropdownOption, FormFieldConfig, FormSection, FormBuilderConfig, FileReference, FormSubmission
  - File imports UUID from @zerobias-org/types-core-js
  - src/app/core/models/index.ts includes `export * from './form-builder.model'`
  - File is valid TypeScript (no syntax errors)
  - All interfaces use correct types (string, UUID, boolean, enum, etc.)
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:

1. Schema PR merged to zerobias-org/schema:dev with FormSubmission and formConfig changes
2. `npm run verify` passes in schema repo
3. FormSubmission class ID captured and documented (needed for Plan 01)
4. TypeScript model interfaces created and exported from models/index.ts
5. No TypeScript compilation errors in src/app/core/models/

**~15 minute wait:** GQL schema reloads on platform after merge. Plan 01 can begin after this window.
</verification>

<success_criteria>
- FormSubmission schema class exists in zerobias-org/schema with all required fields
- SmeMartProject has formConfig JSON field
- Both schema changes pass dataloader validation
- Schema PR merged to dev branch (not main)
- FormSubmission class ID documented for Plan 01 use
- TypeScript model interfaces defined and exported
- No blocking issues preventing Plan 01 execution
</success_criteria>

<output>
After completion, create `.planning/phases/16-form-builder/16-00-SUMMARY.md`
</output>
