---
phase: 10-vendor-profile-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/pages/org/org.component.ts
  - src/app/pages/org/org.routes.ts
  - src/app/pages/org/tabs/vendor-profile-tab.component.ts
  - src/app/pages/org/tabs/vendor-profile-tab.component.html
  - src/app/pages/org/tabs/vendor-profile-tab.component.scss
  - src/app/pages/org/tabs/vendor-profile-form.component.ts
  - src/app/pages/org/tabs/vendor-profile-form.component.html
  - src/app/pages/org/tabs/vendor-profile-form.component.scss
autonomous: true
requirements: [VPU-01, VPU-02, VPU-03, VPU-04, VPU-05, VPU-06, VPU-07]

must_haves:
  truths:
    - Corporate Profile tab is visible on the `/org` page with label "Corporate Profile" and icon "business"
    - Profile items are displayed in 6 accordion panels, one per section (corporate_identity, attestation, insurance, reference, personnel, financial)
    - Each accordion header shows section name, item count, and expired badge (visible only when count > 0)
    - User can click "Add [Section] Item" button on accordion header to open side drawer in create mode
    - User can click "Edit" on any row to open side drawer in edit mode with pre-filled data
    - User can delete items with inline confirmation (row toggles to show Cancel/Delete buttons)
    - Expired items display with EXPIRED status chip (#eed5d1 color) and are NOT hidden
    - Items expiring within 30 days display with EXPIRING_SOON status chip (#ffb74d color)
    - Expired items show optional "Renew" button on the row to quick-edit
    - Side drawer pre-fills current data when editing and accepts form submission
    - Changes appear in the UI within 2-3 seconds of submission (optimistic update after service call)
    - Empty state shows simple "Add [Section] Item" button (per D-15, no welcome card in first iteration)

  artifacts:
    - path: src/app/pages/org/tabs/vendor-profile-tab.component.ts
      provides: Main accordion component with section grouping, item list, side drawer orchestration
      exports: [VendorProfileTab]
    - path: src/app/pages/org/tabs/vendor-profile-form.component.ts
      provides: Reactive form for add/edit with mode-aware pre-fill and section-specific field layouts
      exports: [VendorProfileForm]
    - path: src/app/pages/org/org.component.ts
      provides: Updated tabs array with Corporate Profile entry
      exports: [OrgPage]
    - path: src/app/pages/org/org.routes.ts
      provides: New lazy-loaded route for vendor-profile-tab
      exports: routes

  key_links:
    - from: org.component.ts
      to: vendor-profile-tab.component.ts
      via: lazy-loaded route in org.routes.ts
      pattern: "{ path: 'profile', component: VendorProfileTab }"
    - from: vendor-profile-tab.component.ts
      to: VendorProfileService
      via: service injection and list/create/update/delete calls
      pattern: "this.vendorProfileService.listProfileItems(orgId)"
    - from: vendor-profile-tab.component.ts
      to: vendor-profile-form.component.ts
      via: @Input/@Output bindings for mode, item, section, save, close
      pattern: "[mode]='formMode()' [item]='editingItem()' (save)='onFormSave($event)'"
    - from: vendor-profile-form.component.ts
      to: MarketplaceProfileItem model interfaces
      via: TypeScript typing for InsuranceData, PersonnelData, etc.
      pattern: "form.patchValue(mapItemToForm(item))"
---

<objective>
Implement the Corporate Profile tab on the `/org` page, enabling org members to view, add, edit, and delete marketplace profile items organized by 6 sections (corporate_identity, attestation, insurance, reference, personnel, financial) with expiration indicators and renewal prompts.

Purpose: This completes the UI layer for vendor profile management, consuming the VendorProfileService built in Phase 9 and delivering a production-ready interface for managing supply-side marketplace qualifications.

Output: Three new components (vendor-profile-tab, vendor-profile-form) + route integration, with full CRUD workflow and expiration visual treatment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-vendor-profile-ui/10-CONTEXT.md
@.planning/phases/10-vendor-profile-ui/10-RESEARCH.md

# Phase 9 Summary (direct dependency)
@.planning/phases/09-vendor-profile-service/09-01-SUMMARY.md

# Existing reference implementations (UI patterns)
@src/app/pages/engagements/tabs/vetting-tab.component.ts
@src/app/pages/org/tabs/members-tab.component.ts
@src/app/pages/org/org.component.ts
@src/app/pages/org/org.routes.ts

# Service and Model (Phase 9 output)
@src/app/core/services/vendor-profile.service.ts
@src/app/core/models/marketplace-profile-item.model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vendor-profile-tab main component with accordion panels and item list</name>
  <files>
    src/app/pages/org/tabs/vendor-profile-tab.component.ts
    src/app/pages/org/tabs/vendor-profile-tab.component.html
    src/app/pages/org/tabs/vendor-profile-tab.component.scss
  </files>
  <read_first>
    src/app/pages/engagements/tabs/vetting-tab.component.ts
    src/app/pages/org/tabs/members-tab.component.ts
    src/app/core/services/vendor-profile.service.ts
    src/app/core/models/marketplace-profile-item.model.ts
  </read_first>
  <action>
Create vendor-profile-tab.component.ts as a standalone component (ChangeDetectionStrategy.OnPush) with:
- Inject ZerobiasClientApp, VendorProfileService
- Signal: items = signal<MarketplaceProfileItem[]>([])
- Signal: sidenavOpen = signal(false)
- Signal: formMode = signal<'create' | 'edit'>('create')
- Signal: selectedSection = signal<SectionType>('')
- Signal: editingItem = signal<MarketplaceProfileItem | null>(null)
- Signal: isLoading = signal(false)
- Signal: currentOrgId = signal('')
- Computed signals (per SECTION_ORDER array, hardcoded ['corporate_identity', 'attestation', 'insurance', 'personnel', 'financial', 'reference']):
  - readonly corporateIdentityItems = computed(() => this.items().filter(i => i.section === 'corporate_identity'))
  - (repeat for all 6 sections)
- Computed signals for expiration:
  - readonly isExpired(item): boolean — returns item.expiresAt && new Date(item.expiresAt) < new Date()
  - readonly isExpiringSoon(item): boolean — returns item.expiresAt && daysUntilExpiry > 0 && daysUntilExpiry <= 30
  - readonly expiredCount(section): number — count of expired items in that section
- ngOnInit(): subscribe to getCurrentOrg(), extract orgId, call loadItems()
- loadItems(): call vendorProfileService.listProfileItems(orgId), set items signal, handle errors with console.error
- getStatusLabel(item): return 'EXPIRED' | 'EXPIRING_SOON' | 'ACTIVE' based on expiration helpers
- openAddForm(section: SectionType): set formMode='create', selectedSection, editingItem=null, sidenavOpen=true
- openEditForm(item: MarketplaceProfileItem): set formMode='edit', editingItem, sidenavOpen=true
- async onFormSave(request: CreateMarketplaceProfileItemRequest): 
  - if formMode='create': call vendorProfileService.create(currentOrgId(), request)
  - if formMode='edit': call vendorProfileService.update(editingItem().id, request)
  - on success: call loadItems(), sidenavOpen.set(false)
  - on error: show snackBar.open('Failed to save profile item', 'OK')
- async onDeleteItem(item: MarketplaceProfileItem):
  - call vendorProfileService.delete(item.id)
  - on success: loadItems()
  - on error: snackBar.open('Failed to delete profile item', 'OK')

Imports: CommonModule, MatExpansionModule, MatSidenavModule, MatButtonModule, MatIconModule, MatMenuModule, MatChipsModule, ReactiveFormsModule, VendorProfileService, ZerobiasClientApp, MatSnackBar (inject)

Create vendor-profile-tab.component.html with:
- MatSidenavContainer with flex layout
- MatSidenav #drawer (position="end", [opened]="sidenavOpen()"):
  - <app-vendor-profile-form
      [mode]="formMode()"
      [section]="selectedSection()"
      [item]="editingItem()"
      (save)="onFormSave($event)"
      (close)="sidenavOpen.set(false)">
    </app-vendor-profile-form>
- MatSidenavContent:
  - Loading spinner: @if (isLoading()) { <mat-spinner /> }
  - MatAccordion (multi, allowMultiple):
    - @for (section of ['corporate_identity', 'attestation', ...]; track section) {
      - <mat-expansion-panel>
        - <mat-expansion-panel-header>
          - Section name (titlecase pipe)
          - Item count badge: {{ items().filter(i => i.section === section).length }} items
          - @if (expiredCount(section) > 0) { <mat-chip class="expired-badge">⚠ {{ expiredCount(section) }} expired</mat-chip> }
          - <button mat-icon-button (click)="openAddForm(section)" title="Add {{ section }} Item">
              <mat-icon>add</mat-icon>
            </button>
        - <mat-expansion-panel-content>
          - @for (item of (section === 'corporate_identity' ? corporateIdentityItems() : ...); track item.id) {
            - Row showing: name, key detail from section (e.g., "Policy #ABC123" for insurance), status chip, action menu
            - Status chip: <zb-resource-status [label]="getStatusLabel(item)" [pill]="true" size="small" />
            - Action menu with: Edit, Delete
            - @if (isExpired(item)) { <button mat-stroked-button (click)="openEditForm(item)">Renew</button> }
          - @if (items().filter(i => i.section === section).length === 0) { <p>No items in this section. Click + to add one.</p> }

Create vendor-profile-tab.component.scss with:
- .vendor-profile-container { display: flex; height: 100%; }
- mat-expansion-panel: margin-bottom: 8px
- mat-expansion-panel-header: display flex, justify-content space-between, align-items center
- .section-summary { display flex; align-items center; gap 8px; }
- .item-count { font-size 12px; color var(--mat-sys-on-surface-variant); }
- .expired-badge { background #eed5d1; color #0f0f10; height 24px; }
- .item-row { display flex; justify-content space-between; align-items center; padding 12px; border-bottom 1px solid var(--mat-sys-outline-variant); }
- .item-name { font-weight 500; }
- .item-detail { font-size 12px; color var(--mat-sys-on-surface-variant); }
- .item-status { margin 0 8px; }
- .item-actions { display flex; gap 4px; }
- mat-sidenav { width 400px; }
- .expired-badge { background-color #eed5d1; }
  </action>
  <verify>
    <automated>npm test -- vendor-profile-tab.component.spec.ts --run 2>&1 | grep -E "(PASS|FAIL|✓|✗)"</automated>
  </verify>
  <acceptance_criteria>
    - File vendor-profile-tab.component.ts exists at correct path with export VendorProfileTab
    - File vendor-profile-tab.component.html exists with mat-expansion-panel structure for 6 sections
    - File vendor-profile-tab.component.scss exists with accordion and item row styling
    - Component imports MatExpansionModule, MatSidenavModule, and VendorProfileService
    - Component has computed signals for all 6 sections (corporateIdentityItems, attestationItems, insuranceItems, referenceItems, personnelItems, financialItems)
    - Component has isExpired() and isExpiringSoon() helper methods
    - Component has openAddForm() and openEditForm() methods
    - Component has onFormSave() async method that calls vendorProfileService.create() or .update()
    - Component has loadItems() method that calls vendorProfileService.listProfileItems(orgId)
    - Template shows all 6 sections with item count + expired badge
    - Template has MatSidenavModule outlet for form drawer
  </acceptance_criteria>
  <done>Vendor-profile-tab component created with accordion grouping, section filters, and service integration. Main tab interface ready for form drawer integration.</done>
</task>

<task type="auto">
  <name>Task 2: Create vendor-profile-form side drawer component with reactive forms and section-specific fields</name>
  <files>
    src/app/pages/org/tabs/vendor-profile-form.component.ts
    src/app/pages/org/tabs/vendor-profile-form.component.html
    src/app/pages/org/tabs/vendor-profile-form.component.scss
  </files>
  <read_first>
    src/app/shared/components/vetting-item-dialog/vetting-item-dialog.component.ts
    src/app/core/models/marketplace-profile-item.model.ts
    src/app/pages/org/tabs/vendor-profile-tab.component.ts
  </read_first>
  <action>
Create vendor-profile-form.component.ts as a standalone component (ChangeDetectionStrategy.OnPush) with:
- Inject: FormBuilder, MatSnackBar
- @Input() mode: 'create' | 'edit' = 'create'
- @Input() section: SectionType = 'corporate_identity'
- @Input() item?: MarketplaceProfileItem
- @Output() save = new EventEmitter<CreateMarketplaceProfileItemRequest>()
- @Output() close = new EventEmitter<void>()
- form: FormGroup (created in ngOnInit)
- Signal: submitting = signal(false)

Form structure per section type:
  - All sections: name (required, string), description (optional, string)
  - corporate_identity: legalEntityName (required), businessType, foundedYear, yearsInBusiness, certifications (comma-separated), numberOfEmployees
  - attestation: serviceType (required), yearsExperience (required, number), clientCount, avgProjectDuration, certifications (comma-separated), specializations (comma-separated)
  - insurance: policyNumber (required), carrier (required), coverageType, coverageAmount (required, number), effectiveDate (required, mat-datepicker), expirationDate (required, mat-datepicker), limits, deductible
  - reference: clientName (required), contactPerson (required), email (required, email validator), phone, projectType, projectDuration, outcome
  - personnel: name (required), title (required), yearsExperience (required, number), specialization, credentials (comma-separated), certifications (comma-separated)
  - financial: annualRevenue (required, number), profitMargin, employeeCount, yearsOperating, revenueGrowth

ngOnInit():
- Create FormGroup using FormBuilder with fields for current section
- If mode='edit' and item: patchValue(mapItemToForm(item))
- Helper mapItemToForm(item): deserialize item.data JSON and create form-compatible object

ngOnDestroy(): unsubscribe from observables

onSubmit():
- if form.invalid: return (show validation errors)
- const request = mapFormToRequest(): serialize form values to CreateMarketplaceProfileItemRequest
- set submitting.set(true)
- emit save.emit(request)
- submitting.set(false) on completion (handled by parent)

onCancel():
- emit close.emit()

Helper mapFormToRequest(): convert form.value to CreateMarketplaceProfileItemRequest with:
- name: form.get('name').value
- section: this.section
- data: { ... section-specific fields from form, handling comma-separated arrays }
- expiresAt: expirationDate or null (optional)

Helper mapItemToForm(item): convert item.data back to form-friendly shape:
- For array fields (certifications, credentials): join with ', ' for display in text input
- For expirationDate: format as YYYY-MM-DD for mat-datepicker

Imports: CommonModule, ReactiveFormsModule, MatFormFieldModule, MatInputModule, MatButtonModule, MatDatepickerModule, MatNativeDateModule, MatIconModule, MatSelectModule, VendorProfileService, MarketplaceProfileItem type

Create vendor-profile-form.component.html with:
- Toolbar header: "Add [Section] Item" / "Edit [Section] Item" title, close button (X)
- Scrollable content area:
  - @switch (section()) case for each section type:
    - @case ('insurance'):
      - policyNumber text input (required)
      - carrier text input (required)
      - coverageType text input
      - coverageAmount number input (required)
      - effectiveDate mat-datepicker input (required)
      - expirationDate mat-datepicker input (required)
      - limits text input
      - deductible number input
    - @case ('personnel'):
      - name text input (required)
      - title text input (required)
      - yearsExperience number input (required)
      - specialization text input
      - credentials text input (comma-separated, helper text)
      - certifications text input (comma-separated, helper text)
    - @case ('corporate_identity'):
      - legalEntityName text input (required)
      - businessType text input
      - foundedYear number input
      - yearsInBusiness number input
      - certifications text input (comma-separated)
      - numberOfEmployees number input
    - @case ('attestation'):
      - serviceType text input (required)
      - yearsExperience number input (required)
      - clientCount number input
      - avgProjectDuration text input
      - certifications text input (comma-separated)
      - specializations text input (comma-separated)
    - @case ('reference'):
      - clientName text input (required)
      - contactPerson text input (required)
      - email text input (required, email validator)
      - phone text input
      - projectType text input
      - projectDuration text input
      - outcome text input
    - @case ('financial'):
      - annualRevenue number input (required)
      - profitMargin number input
      - employeeCount number input
      - yearsOperating number input
      - revenueGrowth number input
  - @if (mode === 'edit' && item.expiresAt && isExpired(item)) {
    - <mat-card class="renewal-notice">
      "This item expired on {{ item.expiresAt | date }}. Update with current information."
    - </mat-card>
  - }
- Footer buttons: Cancel, Save (disabled while submitting)

Create vendor-profile-form.component.scss with:
- .form-container { display: flex; flex-direction: column; height: 100%; }
- .form-header { padding: 16px; border-bottom: 1px solid var(--mat-sys-outline-variant); display: flex; justify-content: space-between; align-items: center; }
- .form-header h2 { margin: 0; font-size: 18px; }
- .form-content { flex: 1; overflow-y: auto; padding: 16px; }
- .form-footer { padding: 16px; border-top: 1px solid var(--mat-sys-outline-variant); display: flex; gap: 8px; justify-content: flex-end; }
- mat-form-field { margin-bottom: 16px; }
- .renewal-notice { background-color: #fff3cd; border-left: 4px solid #ffc107; padding: 12px; margin-bottom: 16px; }
  </action>
  <verify>
    <automated>npm test -- vendor-profile-form.component.spec.ts --run 2>&1 | grep -E "(PASS|FAIL|✓|✗)"</automated>
  </verify>
  <acceptance_criteria>
    - File vendor-profile-form.component.ts exists at correct path with export VendorProfileForm
    - File vendor-profile-form.component.html exists with @switch sections for all 6 section types
    - File vendor-profile-form.component.scss exists with form layout styling
    - Component has @Input mode, section, item properties
    - Component has @Output save, close event emitters
    - Component creates ReactiveFormsModule FormGroup in ngOnInit with section-appropriate fields
    - Component has onSubmit() method that emits save.emit() with CreateMarketplaceProfileItemRequest
    - Component has onCancel() method that emits close.emit()
    - Form shows "Add [Section] Item" or "Edit [Section] Item" title based on mode
    - Form fields match section type (insurance has policyNumber, carrier, etc.; personnel has name, title, etc.)
    - Form pre-fills with item data when mode='edit'
    - Form has section-specific validation (required fields per section, email validation for reference.email, etc.)
    - Renewal notice card appears when editing expired insurance/personnel items
  </acceptance_criteria>
  <done>Vendor-profile-form component created with reactive forms, mode-aware pre-fill, and section-specific field layouts. Form ready for integration with tab component.</done>
</task>

<task type="auto">
  <name>Task 3: Add Corporate Profile tab to org.component.ts tabs array and create org.routes.ts lazy-loaded route</name>
  <files>
    src/app/pages/org/org.component.ts
    src/app/pages/org/org.routes.ts
  </files>
  <read_first>
    src/app/pages/org/org.component.ts
    src/app/pages/org/org.routes.ts
  </read_first>
  <action>
In org.component.ts:
- Add new tab entry to the tabs array (maintain existing order: Documents, Engagements, Projects, Members, Settings):
  { label: 'Corporate Profile', icon: 'business', path: 'profile' }
- This makes the tab visible on the /org page with "Corporate Profile" label and business icon

In org.routes.ts (create file if needed):
- Add lazy-loaded route for vendor-profile-tab:
  {
    path: 'profile',
    loadComponent: () => import('./tabs/vendor-profile-tab.component').then(m => m.VendorProfileTab)
  }
- Ensure all other existing routes (documents, engagements, projects, members, settings) remain intact

File org.routes.ts structure (if file needs to be created):
```typescript
import { Routes } from '@angular/router';

export const orgRoutes: Routes = [
  {
    path: 'documents',
    loadComponent: () => import('./tabs/documents-tab.component').then(m => m.DocumentsTab)
  },
  {
    path: 'engagements',
    loadComponent: () => import('./tabs/engagements-tab.component').then(m => m.EngagementsTab)
  },
  {
    path: 'projects',
    loadComponent: () => import('./tabs/projects-tab.component').then(m => m.ProjectsTab)
  },
  {
    path: 'members',
    loadComponent: () => import('./tabs/members-tab.component').then(m => m.MembersTab)
  },
  {
    path: 'settings',
    loadComponent: () => import('./tabs/settings-tab.component').then(m => m.SettingsTab)
  },
  {
    path: 'profile',
    loadComponent: () => import('./tabs/vendor-profile-tab.component').then(m => m.VendorProfileTab)
  }
];
```

Update org.component.ts routing import (if not already present):
- In the component, update route setup to use the orgRoutes
  </action>
  <verify>
    <automated>grep -n "Corporate Profile" src/app/pages/org/org.component.ts</automated>
  </verify>
  <acceptance_criteria>
    - org.component.ts tabs array includes new entry: { label: 'Corporate Profile', icon: 'business', path: 'profile' }
    - Tab appears 6th in the tabs array (after Members, before or after Settings per final ordering decision)
    - org.routes.ts contains lazy-loaded route for 'profile' path with loadComponent pointing to VendorProfileTab
    - All existing routes (documents, engagements, projects, members, settings) remain unchanged
    - No compilation errors when running ng build or npm test
  </acceptance_criteria>
  <done>Corporate Profile tab registered in org page navigation and lazy-loaded route configured. Tab will appear in the org page tab bar.</done>
</task>

<task type="auto">
  <name>Task 4: Implement inline delete confirmation and item row rendering in vendor-profile-tab template</name>
  <files>
    src/app/pages/org/tabs/vendor-profile-tab.component.ts
    src/app/pages/org/tabs/vendor-profile-tab.component.html
    src/app/pages/org/tabs/vendor-profile-tab.component.scss
  </files>
  <read_first>
    src/app/pages/org/tabs/vendor-profile-tab.component.ts
    src/app/pages/org/tabs/vendor-profile-tab.component.html
  </read_first>
  <action>
Update vendor-profile-tab.component.ts to add per-item delete confirmation state:
- Signal: deletingItemId = signal<string | null>(null)
- Update onDeleteItem(item) to:
  - Set deletingItemId.set(item.id) (shows inline confirmation)
  - Wait for user to confirm (actual delete happens in separate method)
- New method: confirmDelete(item):
  - Call vendorProfileService.delete(item.id)
  - On success: loadItems()
  - On error: snackBar.open('Failed to delete profile item', 'OK')
  - Finally: deletingItemId.set(null)
- New method: cancelDelete():
  - deletingItemId.set(null)

Update vendor-profile-tab.component.html to render item rows with inline delete confirmation:
- Inside mat-expansion-panel-content, change @for item loop to show:
  - @if (deletingItemId() === item.id) {
      - Row shows: item name, "Are you sure?" message, Cancel button, Delete button (red)
    } @else {
      - Row shows: item name + key detail, status chip, action menu (Edit, Delete)
      - Edit button: (click)="openEditForm(item)"
      - Delete button: (click)="onDeleteItem(item)"
    }
- For each section, extract key detail from item.data:
  - insurance: show "Policy #{{ item.data.policyNumber }}"
  - personnel: show "{{ item.data.title }}"
  - attestation: show "{{ item.data.yearsExperience }} years"
  - corporate_identity: show "{{ item.data.legalEntityName }}"
  - reference: show "{{ item.data.clientName }}"
  - financial: show "${{ item.data.annualRevenue | currency }}"

Update vendor-profile-tab.component.scss:
- .item-row { padding: 12px; border-bottom: 1px solid var(--mat-sys-outline-variant); display: flex; justify-content: space-between; align-items: center; }
- .item-row.confirming { background-color: #fff3cd; border-left: 4px solid #ffc107; }
- .item-detail { font-size: 12px; color: var(--mat-sys-on-surface-variant); margin-left: 8px; }
- .delete-confirmation { display: flex; gap: 8px; align-items: center; }
- .delete-confirmation button { margin: 0 4px; }
- .delete-btn { background-color: #d32f2f; color: white; }
  </action>
  <verify>
    <automated>grep -n "deletingItemId" src/app/pages/org/tabs/vendor-profile-tab.component.ts</automated>
  </verify>
  <acceptance_criteria>
    - vendor-profile-tab.component.ts has deletingItemId signal
    - vendor-profile-tab.component.ts has confirmDelete(item) and cancelDelete() methods
    - vendor-profile-tab.component.html shows item rows with name + key detail per section
    - vendor-profile-tab.component.html shows inline delete confirmation (row toggles to "Are you sure?" when deletingItemId matches item.id)
    - Item rows show status chip (EXPIRED/EXPIRING_SOON/ACTIVE)
    - Item rows show Edit and Delete buttons in non-confirming state
    - Item rows show Cancel/Delete buttons in confirming state
    - No mat-menu dialogs or modal dialogs for delete confirmation
  </acceptance_criteria>
  <done>Inline delete confirmation and item row rendering implemented per D-09. Users see row-level UI changes without modal dialogs.</done>
</task>

<task type="auto">
  <name>Task 5: Add "Renew" button for expired items and "updated version needed" checklist card</name>
  <files>
    src/app/pages/org/tabs/vendor-profile-tab.component.ts
    src/app/pages/org/tabs/vendor-profile-tab.component.html
    src/app/pages/org/tabs/vendor-profile-tab.component.scss
  </files>
  <read_first>
    src/app/pages/org/tabs/vendor-profile-tab.component.ts
    src/app/pages/org/tabs/vendor-profile-tab.component.html
  </read_first>
  <action>
Update vendor-profile-tab.component.ts:
- Add Signal: dismissedRenewalCard = signal(false)
- Add Computed: expiredItems = computed(() => this.items().filter(i => this.isExpired(i)))
- Add method: dismissRenewalCard(): dismissedRenewalCard.set(true)

Update vendor-profile-tab.component.html:
- Above the accordion, add "updated version needed" checklist card:
  @if (expiredItems().length > 0 && !dismissedRenewalCard()) {
    <mat-card class="renewal-card">
      <mat-card-header>
        <mat-card-title>Items Needing Renewal</mat-card-title>
        <button mat-icon-button (click)="dismissRenewalCard()" class="close-btn">
          <mat-icon>close</mat-icon>
        </button>
      </mat-card-header>
      <mat-card-content>
        <p>The following items have expired and should be updated:</p>
        <ul>
          @for (item of expiredItems(); track item.id) {
            <li>{{ item.name }} ({{ item.section | titlecase }})
              <button mat-stroked-button (click)="openEditForm(item); dismissRenewalCard()">Update Now</button>
            </li>
          }
        </ul>
      </mat-card-content>
    </mat-card>
  }

- On each item row in the non-confirming state, add "Renew" button:
  @if (isExpired(item)) {
    <button mat-stroked-button class="renew-btn" (click)="openEditForm(item)">Renew</button>
  }

Update vendor-profile-tab.component.scss:
- .renewal-card { background-color: #fff3cd; border-left: 4px solid #ffc107; margin-bottom: 16px; }
- .renewal-card mat-card-header { display: flex; justify-content: space-between; align-items: center; }
- .renewal-card mat-card-title { margin: 0; }
- .renewal-card .close-btn { margin: -8px; }
- .renewal-card ul { list-style: none; padding: 0; margin: 8px 0; }
- .renewal-card li { display: flex; justify-content: space-between; align-items: center; padding: 8px 0; border-bottom: 1px solid rgba(0,0,0,0.1); }
- .renew-btn { margin-left: 8px; }
  </action>
  <verify>
    <automated>grep -n "renewalCard\|Renew\|updated version needed" src/app/pages/org/tabs/vendor-profile-tab.component.html</automated>
  </verify>
  <acceptance_criteria>
    - vendor-profile-tab.component.ts has dismissedRenewalCard signal
    - vendor-profile-tab.component.ts has expiredItems computed signal
    - vendor-profile-tab.component.ts has dismissRenewalCard() method
    - vendor-profile-tab.component.html shows renewal card above accordion when expiredItems().length > 0
    - Renewal card lists all expired items with "Update Now" button
    - Renewal card has close button (X) to dismiss
    - Each expired item row shows "Renew" button
    - Clicking "Renew" opens edit drawer with item pre-filled
    - Renewal card is dismissible and doesn't reappear until page reload (or until items are updated)
  </acceptance_criteria>
  <done>Expiration visual treatment complete with renewal card and per-item "Renew" buttons per D-11, D-12, D-13.</done>
</task>

</tasks>

<verification>
Phase 10 completion requires:
1. ✅ Corporate Profile tab visible on `/org` page with label and icon (VPU-01)
2. ✅ Profile items organized into 6 accordion panels per section (VPU-02)
3. ✅ User can add new items via side drawer form (VPU-03)
4. ✅ User can edit items with pre-filled data (VPU-04)
5. ✅ User can delete items with inline confirmation (VPU-05)
6. ✅ Expired items display with EXPIRED status chip (VPU-06)
7. ✅ "Updated version needed" checklist card for expired items (VPU-07)

Manual verification:
- Navigate to `/org` page, verify "Corporate Profile" tab is visible
- Click tab, verify 6 accordion panels appear (collapsed by default)
- Expand insurance panel, click "Add Insurance Item" button, verify side drawer opens
- Fill form fields (policyNumber, carrier, effectiveDate, expirationDate), click Save
- Verify item appears in list and closes drawer
- Click Edit on item, verify form pre-fills with current data
- Change a field, Save, verify updates appear in list within 2-3 seconds
- Click Delete, verify inline confirmation appears (row shows Cancel/Delete buttons)
- Click Delete button, verify item is removed
- Add item with expiresAt date in the past, refresh page, verify EXPIRED chip appears
- Verify renewal card appears above accordion listing all expired items
- Click "Renew" button on expired item or "Update Now" in renewal card, verify drawer opens pre-filled
</verification>

<success_criteria>
Phase 10 is complete when:
- [ ] All 7 VPU requirements (VPU-01 through VPU-07) are met per acceptance criteria
- [ ] `npm test -- vendor-profile-tab.component.spec.ts --run` passes
- [ ] `npm test -- vendor-profile-form.component.spec.ts --run` passes
- [ ] Manual verification checklist passes (all UI flows work end-to-end)
- [ ] No TypeScript compilation errors on `ng build`
- [ ] Phase summary written to `.planning/phases/10-vendor-profile-ui/10-01-SUMMARY.md`
</success_criteria>

<output>
After completion, create `.planning/phases/10-vendor-profile-ui/10-01-SUMMARY.md` with:
- Phase objective completed
- Components created (vendor-profile-tab, vendor-profile-form)
- Requirements met (VPU-01 through VPU-07)
- Key decisions implemented (D-01 through D-15 from CONTEXT.md)
- Known limitations or deferred work (if any)
- Test coverage and manual verification results
</output>
