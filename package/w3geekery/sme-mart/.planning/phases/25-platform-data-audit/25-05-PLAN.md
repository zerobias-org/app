---
phase: 25-platform-data-audit
plan: 05
type: execute
wave: 3
depends_on: [25-01]
files_modified:
  - src/environments/environment.uat.ts
  - .planning/director/PLATFORM-DATA-INVENTORY.md
autonomous: true
requirements_addressed: [PDA-05]
must_haves:
  truths:
    - "Pipeline receiver `43f08afd-...` is live and accepts pushes on UAT"
    - "App code pipelineId references audited and documented"
    - "environment.uat.ts corrected with canonical pipeline UUID"
  artifacts:
    - path: "src/environments/environment.uat.ts"
      provides: "Updated pipelineId field"
      required: true
    - path: ".planning/director/PLATFORM-DATA-INVENTORY.md"
      provides: "Pipeline Health Check Report section"
      required: true
  key_links:
    - from: "src/environments/environment.uat.ts"
      to: ".planning/director/DECISIONS.md"
      via: "pipelineId value locked in DECISIONS"
      pattern: "pipelineId: '43f08afd-7ab9-4e99-a93c-619c46adaabe'"
    - from: ".planning/director/DECISIONS.md"
      to: ".planning/director/bootstrap-w3geekery-engagement.md"
      via: "Pipeline used in walkthrough"
      pattern: "Current SME Mart receiver pipeline (UAT)"
---

<objective>
Verify the current SME Mart Pipeline is live and healthy on UAT (PDA-05), fix the stale pipelineId in environment.uat.ts, and document the health check in the inventory.

Purpose: One-line bounded code change (D-04 carve-out) + pipeline validation. PDA-05 requirement.

Output: Updated environment file + pipeline health check report in index file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/25-platform-data-audit/25-CONTEXT.md
@.planning/phases/25-platform-data-audit/25-RESEARCH.md
@.planning/director/DECISIONS.md
@.planning/director/bootstrap-w3geekery-engagement.md
@src/environments/environment.uat.ts
@CLAUDE.md
</context>

<read_first>
## Pipeline Health Check Context

1. **Three-part check per D-04:**
   - Part 1: Throwaway Pipeline.receive ping on `43f08afd-7ab9-4e99-a93c-619c46adaabe` confirms live
   - Part 2: grep app code for `pipelineId` references and document mismatches
   - Part 3: One-line env-file fix (change `f6d1f579-...` to `43f08afd-...`)

2. **Current state (from DECISIONS.md):**
   - **Current live pipeline (v1.3 carry-forward, 2026-03-30 cutover):** `43f08afd-7ab9-4e99-a93c-619c46adaabe`
   - **Old stale value in environment.uat.ts:** `f6d1f579-fe02-4158-b99e-a55113fd70cb` (v1.2 receiver)
   - **Boundary (W3Geekery SME Marketplace DEV):** `c15fb2dc-4f8c-48b5-b27a-707bd516b005`

3. **Pipeline.receive structure (from bootstrap walkthrough):**
   - Accepts a batch payload with `data: []` (array of objects) and optional `markDeleted: []`
   - Must be non-empty (pipeline rejects empty data[] even if markDeleted has items)
   - For health ping, use a minimal throwaway class object (e.g., a SmeMartProject stub)

4. **MCP profile:** If still locked from Plans 02–03, use existing. Otherwise: `~/.claude/scripts/zb-mcp-profile-lock.sh check uat-clark@w3geekery` and acquire if needed.

5. **grep targets:**
   - `src/environments/environment*.ts` (all environment configs)
   - `src/app/**/*.ts` for any hardcoded `pipelineId` references (should not exist, but audit)
   - Build configs, scripts, docs that may reference pipeline UUID

6. **One-line change:** In `src/environments/environment.uat.ts` line 15, change:
   ```
   pipelineId: 'f6d1f579-fe02-4158-b99e-a55113fd70cb',
   ```
   to:
   ```
   pipelineId: '43f08afd-7ab9-4e99-a93c-619c46adaabe',
   ```
   Nothing else changes.
</read_first>

<acceptance_criteria>
- [ ] Pipeline health ping succeeds (200 response from Pipeline.receive on `43f08afd-...`)
- [ ] Ping response documented in index file's "Pipeline Health Check Report" section
- [ ] grep finds only expected environment.uat.ts reference to old pipelineId (no app code hardcodes)
- [ ] grep scan documented in index file
- [ ] environment.uat.ts changed: line 15 now has `43f08afd-7ab9-4e99-a93c-619c46adaabe`
- [ ] Old value `f6d1f579-fe02-4158-b99e-a55113fd70cb` no longer present in environment.uat.ts
- [ ] Verification grep: `grep "43f08afd-7ab9-4e99-a93c-619c46adaabe" src/environments/environment.uat.ts` exits 0
- [ ] Verification grep: `grep "f6d1f579" src/environments/environment.uat.ts` exits 1 (old value gone)
- [ ] Index file has "Pipeline Health Check Report" section with: ping result, grep scan results, env-fix commit hash
- [ ] All files committed to git
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Run pipeline health check ping</name>
  <files>.planning/director/PLATFORM-DATA-INVENTORY.md</files>
  <read_first>
    - DECISIONS.md § "Default ZB Engagement Bootstrap" (pipeline UUID, class UUIDs for SmeMartProject)
    - .planning/director/bootstrap-w3geekery-engagement.md § "Step C: Push Engagement record" (Pipeline.receive payload structure)
    - MEMORY.md § "ZeroBias MCP Parameter Patterns" (parameter nesting, required fields)
  </read_first>
  <action>
Execute a throwaway Pipeline.receive push to confirm the pipeline is live on UAT.

**Steps:**
1. Acquire profile lock (if not already held): `~/.claude/scripts/zb-mcp-profile-lock.sh check uat-clark@w3geekery`. If locked elsewhere, wait or release and re-acquire.

2. Construct minimal throwaway SmeMartProject payload:
   ```json
   {
     "pipelineId": "43f08afd-7ab9-4e99-a93c-619c46adaabe",
     "boundaryId": "c15fb2dc-4f8c-48b5-b27a-707bd516b005",
     "classId": "<SmeMartProject-class-UUID>",
     "data": [
       {
         "id": "<throwaway-uuid>",
         "name": "HEALTH-CHECK-PING-PHASE-25",
         "status": "active",
         "projectType": "project"
       }
     ]
   }
   ```

3. Call via MCP: `mcp__zerobias__zerobias_execute service=platform operation=Pipeline.receive` with the above payload.

4. Capture response: 
   - If 200 + success: document the response (timestamp, record ID created)
   - If error: document the error message and investigate (check class ID, boundary scoping, required fields)

5. **DO NOT delete the throwaway record** — Phase 26's cleanup task (per phase-26-brief.md) or a later cleanup batch will handle residue.

**Document in Pipeline Health Check Report section of index:**
```
## Pipeline Health Check Report

**Status:** ✅ Live

**Verified:** YYYY-MM-DD

**Ping Test:**
- Pipeline UUID: `43f08afd-7ab9-4e99-a93c-619c46adaabe`
- Test record: SmeMartProject "HEALTH-CHECK-PING-PHASE-25"
- Response: 200 OK (record materialized with ID `<id>`)
- Timestamp: YYYY-MM-DD HH:MM:SS UTC

**Implication:** The pipeline is accepting writes and persisting records to AuditgraphDB. Phase 28 onwards can use this pipeline for Engagement/SmeMartProject pushes.
```
  </action>
  <verify>
    <automated>grep -q "Pipeline Health Check Report" .planning/director/PLATFORM-DATA-INVENTORY.md && grep -q "43f08afd" .planning/director/PLATFORM-DATA-INVENTORY.md && grep -q "✅ Live" .planning/director/PLATFORM-DATA-INVENTORY.md</automated>
  </verify>
  <done>Pipeline health check ping executed, response documented in index file</done>
</task>

<task type="auto">
  <name>Task 2: Scan app code for pipelineId references</name>
  <files>.planning/director/PLATFORM-DATA-INVENTORY.md</files>
  <read_first>
    - src/environments/ (all environment*.ts files)
    - CLAUDE.md (project structure)
  </read_first>
  <action>
Run grep scan for all `pipelineId` references in the codebase.

**Commands:**
```bash
# Find all pipelineId references
grep -rn "pipelineId" src/ --include="*.ts" --include="*.js" --include="*.json" 2>/dev/null || true
```

**Expected results:**
- `src/environments/environment.uat.ts` line 15: old value `f6d1f579-fe02-4158-b99e-a55113fd70cb`
- `src/environments/environment.prod.ts` (if exists): may have different pipeline ID (check if prod env is separated)
- `src/environments/environment.ts` or `environment.dev.ts` (if exists): may have different/placeholder value
- App code should NOT have any hardcoded pipelineId (should be injected from environment)

**Document in index file:**
```
**Config Scan Results:**

- `src/environments/environment.uat.ts` line 15: `f6d1f579-fe02-4158-b99e-a55113fd70cb` (OLD — will be fixed in Task 3)
- [other environment files: list any other references + their values]
- App code (`src/app/**`): No hardcoded pipelineId references found ✓

**Mismatches:** The only mismatch is the stale value in environment.uat.ts, which Task 3 will correct.
```
  </action>
  <verify>
    <automated>grep -rn "pipelineId" src/environments/environment.uat.ts 2>/dev/null | grep -q "f6d1f579" && echo "Old value still present, needs fix in Task 3" && exit 0 || exit 1</automated>
  </verify>
  <done>Grep scan executed, all pipelineId references documented, mismatches identified</done>
</task>

<task type="auto">
  <name>Task 3: Apply one-line environment.uat.ts fix</name>
  <files>src/environments/environment.uat.ts</files>
  <read_first>
    - src/environments/environment.uat.ts (current state)
    - DECISIONS.md "Default ZB Engagement Bootstrap" (canonical pipeline UUID for UAT)
  </read_first>
  <action>
Edit `src/environments/environment.uat.ts` line 15. Replace:
```
  pipelineId: 'f6d1f579-fe02-4158-b99e-a55113fd70cb',
```
with:
```
  pipelineId: '43f08afd-7ab9-4e99-a93c-619c46adaabe',
```

This is the ONLY change — no other lines touched. Commit with message:

```
fix(phase-25): update UAT pipeline ID to current receiver

Changed pipelineId in environment.uat.ts from v1.2 carry-forward
(f6d1f579...) to current v1.3+ receiver (43f08afd...).

Verified live via Pipeline.receive ping test (Phase 25 health check).

Session: claude --resume poc/sme-mart
```
  </action>
  <verify>
    <automated>grep "43f08afd-7ab9-4e99-a93c-619c46adaabe" src/environments/environment.uat.ts && ! grep "f6d1f579" src/environments/environment.uat.ts</automated>
  </verify>
  <done>environment.uat.ts updated, old value removed, new value committed</done>
</task>

<task type="auto">
  <name>Task 4: Document health check completion in index</name>
  <files>.planning/director/PLATFORM-DATA-INVENTORY.md</files>
  <read_first>
    - .planning/director/PLATFORM-DATA-INVENTORY.md (index file, Pipeline Health Check section)
    - Tasks 1–3 outputs
  </read_first>
  <action>
Finalize the "Pipeline Health Check Report" section in the index file with:

1. **Ping test result:** Status ✅ Live, timestamp, record ID created
2. **Config scan results:** All references found, mismatches (if any) and their resolution
3. **Fix applied:** environment.uat.ts line 15, commit hash from Task 3
4. **Next steps:** Phase 28+ can use this pipeline for pushes; no further action needed for Phase 25

Example final section:
```markdown
## Pipeline Health Check Report

**Status:** ✅ Live on UAT

**Verified:** YYYY-MM-DD

**Part 1: Receive Ping Test**
- Pipeline: `43f08afd-7ab9-4e99-a93c-619c46adaabe`
- Test payload: SmeMartProject "HEALTH-CHECK-PING-PHASE-25"
- Response: 200 OK, record ID `<id>`

**Part 2: Config Scan**
- `src/environments/environment.uat.ts` line 15: OLD value `f6d1f579-...` (resolved in Part 3)
- Other environment files: [list + values]
- App code: No hardcoded references ✓

**Part 3: Fix Applied**
- Commit: [commit hash] — updated environment.uat.ts pipelineId to `43f08afd-...`

**Implication:**
The current pipeline is live and accepting writes. All Phase 25 requirements (PDA-05) satisfied. Phase 28+ can proceed with Engagement/SmeMartProject pushes via Pipeline.receive.
```
  </action>
  <verify>
    <automated>grep -A 20 "## Pipeline Health Check Report" .planning/director/PLATFORM-DATA-INVENTORY.md | grep -q "43f08afd" && grep -q "✅ Live" .planning/director/PLATFORM-DATA-INVENTORY.md</automated>
  </verify>
  <done>Health check report section completed and documented in index file</done>
</task>

</tasks>

<verification>
**Post-execution verification:**
1. Pipeline.receive ping succeeds with 200 response
2. Health check report documented in index with: ping result, timestamp, record ID
3. grep scan completed, all pipelineId references found and documented
4. environment.uat.ts line 15 contains new pipeline UUID `43f08afd-...`
5. environment.uat.ts line 15 no longer contains old UUID `f6d1f579-...`
6. Commit message includes phase 25 reference + session name
7. `verify-inventory.sh` runs successfully
8. Index file finalized with all sections complete

**Manual verification:**
```bash
grep "43f08afd-7ab9-4e99-a93c-619c46adaabe" src/environments/environment.uat.ts  # Should exit 0
grep "f6d1f579" src/environments/environment.uat.ts  # Should exit 1 (not found)
```
</verification>

<success_criteria>
- [ ] Pipeline.receive ping succeeds (200 response)
- [ ] Ping result documented in index file with timestamp + record ID
- [ ] grep scan completed, all pipelineId references documented
- [ ] environment.uat.ts pipelineId changed to `43f08afd-7ab9-4e99-a93c-619c46adaabe`
- [ ] Old value `f6d1f579-...` no longer in environment.uat.ts
- [ ] Commit includes "fix(phase-25): update UAT pipeline ID" message
- [ ] Index file has complete "Pipeline Health Check Report" section
- [ ] `verify-inventory.sh` runs successfully
- [ ] All files committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/25-platform-data-audit/25-05-SUMMARY.md` documenting:
- Pipeline health check: Live ✅, timestamp, record ID
- Config scan: 1 stale reference (fixed), 0 app code hardcodes
- Environment fix: one-line change, commit hash
- PDA-05 requirement: SATISFIED
- Phase 25 complete: All 5 plans delivered, all requirements met, phase ready for handoff
</output>
