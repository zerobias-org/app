---
phase: 25-platform-data-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/director/PLATFORM-DATA-INVENTORY.md
  - .planning/director/platform-data-inventory/_TEMPLATE.md
  - .planning/phases/25-platform-data-audit/scripts/verify-inventory.sh
autonomous: true
requirements_addressed: [PDA-01]
must_haves:
  truths:
    - "Verification script exists and is executable"
    - "Per-source sub-file template exists with required sections"
    - "Index file created with skeleton structure"
  artifacts:
    - path: ".planning/director/PLATFORM-DATA-INVENTORY.md"
      provides: "Index of all platform data sources, pre-fill map, known-unknowns, pipeline health check"
      min_sections: 5
    - path: ".planning/director/platform-data-inventory/_TEMPLATE.md"
      provides: "Template for per-source audit sub-files"
      min_sections: 7
    - path: ".planning/director/platform-data-inventory/"
      provides: "Directory for per-source sub-files"
      required: true
    - path: ".planning/phases/25-platform-data-audit/scripts/verify-inventory.sh"
      provides: "Bash script to verify artifact completeness"
      required: true
  key_links:
    - from: ".planning/director/PLATFORM-DATA-INVENTORY.md"
      to: ".planning/director/platform-data-inventory/"
      via: "Index file links to per-source sub-files"
      pattern: "Links to *.md files in subdirectory"
    - from: ".planning/director/platform-data-inventory/"
      to: ".planning/director/platform-data-inventory/_TEMPLATE.md"
      via: "Template serves as baseline for all source files"
      pattern: "All source files follow the template structure"
---

<objective>
Create infrastructure for Phase 25 Platform Data Audit: verification script, template for per-source documentation, and skeleton index.

Purpose: Set up the scaffolding so subsequent audit tasks (02, 03) produce consistent, verifiable artifacts.

Output: Three files (index, template, script) + one directory. All required by Phase 25 verification contract (25-VALIDATION.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-platform-data-audit/25-CONTEXT.md
@.planning/phases/25-platform-data-audit/25-RESEARCH.md
@.planning/phases/25-platform-data-audit/25-VALIDATION.md
@.planning/director/DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verification script and directory structure</name>
  <files>.planning/phases/25-platform-data-audit/scripts/verify-inventory.sh</files>
  <read_first>
    - .planning/phases/25-platform-data-audit/25-VALIDATION.md (Wave 0 requirements, verification contracts)
    - .planning/director/PLATFORM-DATA-INVENTORY.md (to understand index file location)
  </read_first>
  <action>
Create bash script `.planning/phases/25-platform-data-audit/scripts/verify-inventory.sh` that performs the Wave 0 artifact checks:

1. Check that `.planning/director/PLATFORM-DATA-INVENTORY.md` file exists (exit 1 if missing)
2. Check that `.planning/director/platform-data-inventory/` directory exists (exit 1 if missing)
3. Check that `.planning/director/platform-data-inventory/_TEMPLATE.md` file exists (exit 1 if missing)
4. Count markdown files in `.planning/director/platform-data-inventory/` matching pattern `*.md` (excluding _TEMPLATE.md) and report the count. Exit 0 if count ≥ 0 (initially empty is OK for Wave 1).
5. Verify index file contains section headers: "## Platform Data Inventory", "## Pre-fill Map", "## Known Unknowns", "## Pipeline Health Check" (exit 1 if ANY missing)
6. Exit 0 on success, non-zero on any failure

Script must be idempotent — run multiple times with same result. Make it executable: `chmod +x`.
  </action>
  <verify>
    <automated>test -x .planning/phases/25-platform-data-audit/scripts/verify-inventory.sh && bash .planning/phases/25-platform-data-audit/scripts/verify-inventory.sh</automated>
  </verify>
  <done>Verification script exists, is executable, runs without error. Directory structure created.</done>
</task>

<task type="auto">
  <name>Task 2: Create per-source template file</name>
  <files>.planning/director/platform-data-inventory/_TEMPLATE.md</files>
  <read_first>
    - .planning/phases/25-platform-data-audit/25-RESEARCH.md (§2 Per-Source Sub-File Template — the locked structure)
    - .planning/director/DECISIONS.md (context on Object.tag, Pipeline patterns, GQL syntax)
  </read_first>
  <action>
Create file `.planning/director/platform-data-inventory/_TEMPLATE.md` with the following structure (locked per RESEARCH.md §2):

```
---
source: <SDK_method_or_GQL_query_or_hydra_method>
surface: SDK | GQL | hydra | portal-curl
verified: YYYY-MM-DD
uat_tested: true|false
---

## Signature

<exact method signature, return type, required params, optional params>

## Sample Response (W3Geekery, real values)

<actual API response (JSON or markdown table), truncate if >500 lines with note + line count>

## Field List

| Field | Type | Always Populated? | Org-Scoped? | Notes |
|-------|------|-------------------|-------------|-------|
| <fieldName> | <type> | yes/no/sometimes | yes/no/conditional | <reason if sparse or conditional> |

## Pre-fill Map Contributions

Which Phase 28 form fields source from this call:
- `field_name` ← `Source.path` (fully pre-fillable | partial | no)

## Known Gaps / Edge Cases

- What's missing, what's null, what's conditional
- Org-scoping behavior
- Latency / pagination notes

## Write-Path Target (D-12)

Which Platform class + field or SDK setter does Phase 28 write to?
- e.g., `Pipeline.receive` for Engagement.class / `name` field
```

This is the standard layout all source sub-files will follow. No content yet — template only.
  </action>
  <verify>
    <automated>test -f .planning/director/platform-data-inventory/_TEMPLATE.md && grep -q "## Signature" .planning/director/platform-data-inventory/_TEMPLATE.md && grep -q "## Sample Response" .planning/director/platform-data-inventory/_TEMPLATE.md</automated>
  </verify>
  <done>Template file exists with all 7 required sections (frontmatter, Signature, Sample Response, Field List, Pre-fill Map Contributions, Known Gaps, Write-Path Target)</done>
</task>

<task type="auto">
  <name>Task 3: Create index file skeleton</name>
  <files>.planning/director/PLATFORM-DATA-INVENTORY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (PDA-01 requirement: index file must exist at this path)
    - .planning/phases/25-platform-data-audit/25-RESEARCH.md (§8 Pre-Fill Map Column Schema for locked column structure)
    - .planning/director/phase-28-brief.md (to understand what form fields Phase 28 will enumerate)
  </read_first>
  <action>
Create file `.planning/director/PLATFORM-DATA-INVENTORY.md` with the following skeleton structure. This index is the ONE file consumers (Phase 26, Phase 27, Phase 28, Phase 31) will read — it links to per-source sub-files:

```markdown
---
phase: 25-platform-data-audit
verified: YYYY-MM-DD
source_files_count: 0 (will increase as sources are audited)
---

# Platform Data Audit — ZeroBias SDK/GQL Inventory

## Executive Summary

Complete catalog of ZeroBias SDK, GraphQL, and hydra APIs that expose data about an authenticated customer Org. Used by downstream phases (especially Phase 28 Company Profile Form) to determine what org data is pre-fillable vs. requires user input vs. needs enrichment.

**Audit methodology:** Live MCP calls against W3Geekery test org (per D-05, CONTEXT.md). No cached responses. Re-run against Neon + AuditgraphDB via the appropriate SDK/GQL client.

**Deliverables:**
1. This index file (PLATFORM-DATA-INVENTORY.md)
2. Per-source sub-files at `platform-data-inventory/<source>.md`
3. Pre-fill map (below)
4. Known-unknown list (below)
5. Pipeline health check report (below)

---

## Audit Coverage

### Mandatory Sources (9 required, per PDA-02 and CONTEXT.md D-02)

| # | Source | Surface | Brief | Sub-file | Status |
|---|--------|---------|-------|----------|--------|
| 1 | `dana.User.getWhoAmI` / `getPrincipal` | SDK | Current authenticated user + org + admin flag | `whoami.md` | ⬜ pending |
| 2 | `dana.Org.getCurrentOrg` | SDK | Current org metadata (name, description, logo, etc.) | `currentorg.md` | ⬜ pending |
| 3 | `dana.Org.search` / `dana.Org.get` | SDK | Query orgs by ID or search | `orgsearch.md` | ⬜ pending |
| 4 | `dana.User.search` / `dana.User.listUsers` | SDK | Users in current org | `usersearch.md` | ⬜ pending |
| 5 | `platform.Boundary.list` / `platform.Boundary.get` | SDK | Boundaries accessible to current user | `boundary.md` | ⬜ pending |
| 6 | `platform.Task.list` / `platform.Task.search` | SDK | Task search + list | `task.md` | ⬜ pending |
| 7 | `graphql.Boundary.boundaryExecuteRawQuery` | GQL | Direct class-Object queries (Engagement, SmeMartProject, MarketplaceProfileItem) | `gql-class-objects.md` | ⬜ pending |
| 8 | `hydra.Tag.listTags` / `hydra.Tag.searchTags` / `hydra.Tag.getTag` | hydra API | Tag discovery + metadata | `hydra-tag.md` | ⬜ pending |
| 9 | `hydra.Resource.listResources` / `hydra.Resource.searchResources` / `hydra.Resource.getResource` | hydra API | Resource search + retrieval | `hydra-resource.md` | ⬜ pending |

### Opportunistic Expansion (D-02 aggressive discovery)

Likely additional sources to be audited if discovered during mandatory audit. Candidates from RESEARCH.md §3:

| # | Source | Surface | Rationale |
|---|--------|---------|-----------|
| 10 | `dana.Org.listMembers` | SDK | Org membership + roles |
| 11 | `platform.MarketplaceProfileItem.*` | SDK | Vendor/buyer profile CRUD |
| 12 | `platform.Class.getClass` / `platform.Class.getClassObjects` | SDK | Class metadata + object discovery |
| 13 | `platform.Object.getVersionByObjectIdOrVersionId` | SDK | Post-ingest object read by UUID |
| 14 | `portal.Product.search` / `portal.Framework.list` | Portal API | Catalog entities (products, frameworks) |
| 15 | `hydra.Resource.tagResource` / `hydra.Resource.linkResources` | hydra API | Write-side discovery |

---

## Pre-fill Map

**Form-field-first orientation** (per D-08, D-12 — Phase 28 consumption model). Locked columns per RESEARCH.md §8.

| Phase 28 Form Field | Source Call | Platform Field Path | Pre-fillable | Save Target | Notes |
|---|---|---|---|---|---|
| (rows populated during audit tasks 02, 03) | | | | | |

---

## Known Unknowns

Fields that Phase 28 form needs but cannot pre-populate from platform. Flat list with "why" note per field (per D-03):

- (To be populated during audit tasks 02, 03)

---

## Pipeline Health Check Report

**Status:** ⬜ pending

Three-part check (D-04):
1. **Receive ping:** `Pipeline.receive` on `43f08afd-7ab9-4e99-a93c-619c46adaabe` (current UAT pipeline) — confirm live, document response
2. **App-code scan:** grep app for `pipelineId` references and document mismatches against current live value
3. **Env-file fix:** Apply one-line fix to `src/environments/environment.uat.ts` (change pipelineId from old value to new)

Report details populated during Plan 05 (pipeline health check task).

---

## References

- **CONTEXT.md:** Locked decisions (D-01 through D-12), coverage strategy, methodology
- **RESEARCH.md:** Per-source template (§2), pre-fill map columns (§8), draft company_info convention shape (§9)
- **VALIDATION.md:** Verification contract, per-task verification map, manual checks
- **DECISIONS.md:** Object.tag field shape, default ZB engagement bootstrap recipe, pipeline UUIDs
- **bootstrap-w3geekery-engagement.md:** Canonical walkthrough with W3Geekery UUIDs
- **phase-28-brief.md:** Form field enumeration (primary consumer of pre-fill map)
- **phase-26-brief.md:** company_info convention ratification (consumes Phase 25 draft)
```

Fill in `verified: YYYY-MM-DD` with today's date when creating. Leave sub-file rows as "⬜ pending" — they'll populate during Plans 02–03 as sources are audited.
  </action>
  <verify>
    <automated>test -f .planning/director/PLATFORM-DATA-INVENTORY.md && grep -q "## Pre-fill Map" .planning/director/PLATFORM-DATA-INVENTORY.md && grep -q "## Known Unknowns" .planning/director/PLATFORM-DATA-INVENTORY.md && grep -q "## Pipeline Health Check" .planning/director/PLATFORM-DATA-INVENTORY.md</automated>
  </verify>
  <done>Index file exists with all required section headers. Template table structures in place. Ready for content population by subsequent tasks.</done>
</task>

</tasks>

<verification>
All Wave 0 prerequisites met:
- `verify-inventory.sh` script exists, is executable, passes all checks
- `_TEMPLATE.md` follows locked format from RESEARCH.md §2
- Index file (`PLATFORM-DATA-INVENTORY.md`) created with skeleton structure at correct path
- Directory `.planning/director/platform-data-inventory/` created and ready for per-source sub-files
</verification>

<success_criteria>
- [ ] `verify-inventory.sh` exits 0 when run
- [ ] `_TEMPLATE.md` contains all 7 required sections
- [ ] `PLATFORM-DATA-INVENTORY.md` exists with all 5 required section headers
- [ ] All three files are in correct locations per PDA-01
- [ ] Files are committed to git (no uncommitted changes)
</success_criteria>

<output>
After completion, create `.planning/phases/25-platform-data-audit/25-01-SUMMARY.md` documenting:
- Verification script location + sample output
- Template structure + link to RESEARCH.md reference
- Index file location + link to downstream consumers (Phase 26, 28)
- Next steps: Plan 02 (SDK audit) and Plan 03 (GQL audit) will populate the index rows
</output>
