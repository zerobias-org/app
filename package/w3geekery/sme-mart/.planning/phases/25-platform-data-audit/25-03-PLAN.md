---
phase: 25-platform-data-audit
plan: 03
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - .planning/director/platform-data-inventory/gql-class-objects.md
  - .planning/director/platform-data-inventory/hydra-tag.md
  - .planning/director/platform-data-inventory/hydra-resource.md
autonomous: true
requirements_addressed: [PDA-02]
must_haves:
  truths:
    - "GQL class-object queries documented with sample responses"
    - "hydra Tag API documented"
    - "hydra Resource API documented"
    - "Pre-fill map contributions identified for each source"
  artifacts:
    - path: ".planning/director/platform-data-inventory/gql-class-objects.md"
      provides: "GQL queries for Engagement, SmeMartProject, MarketplaceProfileItem, Bid"
      min_sections: 7
    - path: ".planning/director/platform-data-inventory/hydra-tag.md"
      provides: "Tag discovery via hydra API"
      min_sections: 7
    - path: ".planning/director/platform-data-inventory/hydra-resource.md"
      provides: "Resource discovery via hydra API"
      min_sections: 7
  key_links:
    - from: ".planning/director/platform-data-inventory/gql-class-objects.md"
      to: ".planning/director/PLATFORM-DATA-INVENTORY.md"
      via: "Primary read source for company profile data"
      pattern: "MarketplaceProfileItem fields feed pre-fill map"
    - from: ".planning/director/DECISIONS.md"
      to: ".planning/director/platform-data-inventory/gql-class-objects.md"
      via: "RFC4515 filter syntax documented in DECISIONS"
      pattern: "GQL filter examples use .eq. dot-prefix syntax"
---

<objective>
Audit the 3 remaining mandatory sources: GQL class-object queries (Boundary.boundaryExecuteRawQuery for Engagement, SmeMartProject, MarketplaceProfileItem, Bid), hydra Tag API, hydra Resource API. These sources provide application-domain data and cross-org discovery.

Purpose: Complete the "GQL + hydra" portion of the mandatory 9-source audit (PDA-02). These sources are critical for pre-filling company profile fields from the platform data model.

Output: Three sub-files at `platform-data-inventory/{gql-class-objects,hydra-tag,hydra-resource}.md`, each following the locked template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/25-platform-data-audit/25-CONTEXT.md
@.planning/phases/25-platform-data-audit/25-RESEARCH.md
@.planning/director/DECISIONS.md
@.planning/director/bootstrap-w3geekery-engagement.md
@.planning/director/platform-data-inventory/_TEMPLATE.md
@.planning/director/platform-data-inventory/whoami.md (from Plan 02)
@CLAUDE.md
@.claude/notes/zb-graphql-custom-schema-howto.md
</context>

<read_first>
## Critical Context Before Audit

1. **Profile lock required:** Use same lock from Plan 02 or re-acquire: `~/.claude/scripts/zb-mcp-profile-lock.sh check uat-clark@w3geekery`. Maintain lock through Plan 04 if possible to avoid lock churn.

2. **W3Geekery test org IDs (from DECISIONS.md):**
   - Org UUID: `cd7105df-523d-5392-9f9a-3f83d3f30107`
   - Boundary UUID: `c15fb2dc-4f8c-48b5-b27a-707bd516b005`
   - Default Engagement external UUID: `746010b7-dc99-436b-9142-8c4b85c5e623`
   - Default SmeMartProject external UUID: `ea4db55f-2c57-4567-a1be-6e7fd1a210bf`
   - Engagement tag UUID: `a81cd320-243e-44eb-bdd9-9824019ef3dd`

3. **RFC4515 filter syntax (per DECISIONS.md refinement #12):**
   - GQL accepts dot-prefix operators in filter values: `.eq.`, `.sw.`, `.in.`, etc.
   - For tag field specifically: `{ value: ".eq.<tag-uuid>" }`
   - Example: `Engagement(tag: { value: ".eq.a81cd320-243e-44eb-bdd9-9824019ef3dd" }) { ... }`

4. **MCP discipline:** Use `mcp__zerobias__zerobias_describe` for unfamiliar GQL queries. GQL operations are typically under `service=graphql` or `service=boundary` depending on ZB SDK naming.

5. **Class IDs (from DECISIONS.md / project memory):**
   - Engagement class UUID: TBD by zerobias_describe (use bootstrap walkthrough as reference)
   - SmeMartProject class UUID: TBD by zerobias_describe
   - MarketplaceProfileItem class UUID: TBD by zerobias_describe
   - These are deterministic across prod + UAT per memory

6. **Template locked:** Same 7-section structure as Plan 02.

7. **Verified date:** Use today's date in `verified: YYYY-MM-DD` frontmatter.

8. **Aggressive expansion (D-02):** If during GQL audit you discover MarketplaceProfileItem variants (buyer vs. provider profile, different field structures), document all. Ditto for hydra.

9. **Phase 28 form field context (repeated for completeness):** Form fields that GQL must pre-populate:
   - legal_name, dba, logo_url, short_blurb, long_description, primary_contact, website, hq_location, years_in_business, employee_count
   - These fields come primarily from MarketplaceProfileItem (buyer profile record)
</read_first>

<acceptance_criteria>
- [ ] All three sub-files follow template structure (7 sections each)
- [ ] Sample GQL responses show real W3Geekery Engagement/SmeMartProject/MarketplaceProfileItem objects (UUIDs match DECISIONS.md)
- [ ] Field lists complete for Engagement, SmeMartProject, MarketplaceProfileItem, and key hydra entities
- [ ] RFC4515 filter syntax examples included in gql-class-objects.md
- [ ] Pre-fill contributions clearly map MarketplaceProfileItem fields to Phase 28 form fields
- [ ] hydra-tag and hydra-resource sub-files document discovery + link mechanisms
- [ ] Write-path targets identified (which class fields are writable, which hydra operations exist)
- [ ] Known gaps documented (missing fields, null values, limitations)
- [ ] `verify-inventory.sh` runs and counts ≥9 sub-files in platform-data-inventory/
- [ ] All files committed with phase 25 session message
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Audit GQL Boundary.boundaryExecuteRawQuery for class objects</name>
  <files>.planning/director/platform-data-inventory/gql-class-objects.md</files>
  <read_first>
    - .planning/director/platform-data-inventory/_TEMPLATE.md
    - DECISIONS.md § "Object.tag Field Shape — Validated via UAT Experiment" (RFC4515 syntax, tag field discovery)
    - .planning/director/bootstrap-w3geekery-engagement.md § "Step G Front 1" (GQL query example + field shapes)
    - .claude/notes/zb-graphql-custom-schema-howto.md (RFC4515 filter syntax reference)
  </read_first>
  <action>
Run live MCP audit of `graphql.Boundary.boundaryExecuteRawQuery` against W3Geekery boundary. This is the primary GQL entry point for class-object queries.

**MCP calls:**
1. `mcp__zerobias__zerobias_describe path=/graphql/Boundary` to discover boundaryExecuteRawQuery signature and Input type structure
2. `mcp__zerobias__zerobias_execute service=graphql operation=Boundary.boundaryExecuteRawQuery` with:
   - Boundary ID: `c15fb2dc-4f8c-48b5-b27a-707bd516b005`
   - Input: Raw GQL query structure. Example:
     ```
     {
       "query": "{ Engagement(id: \".eq.746010b7-dc99-436b-9142-8c4b85c5e623\") { id, name, engagementTag, dateCreated, dateLastModified, buyerZerobiasOrgId, buyerZerobiasUserId, tag { value } } }"
     }
     ```
3. For SmeMartProject: similar query with `SmeMartProject(...)` class
4. For MarketplaceProfileItem: query both via standard class query and try any vendor/buyer-specific variants
5. Document pagination (pageNumber, pageSize) if discovered

**Document in gql-class-objects.md:**
- Signature: boundaryExecuteRawQuery(boundary: UUID, query: string) → GQL response
- Sample responses: real Engagement, SmeMartProject, MarketplaceProfileItem objects from W3Geekery boundary
- Field lists for EACH class (Engagement fields, SmeMartProject fields, MarketplaceProfileItem fields, Bid fields if time permits)
  - Engagement: id, name, engagementTag, dateCreated, dateLastModified, buyerZerobiasOrgId, buyerZerobiasUserId, tag array
  - SmeMartProject: id, name, description, status, projectType, engagementId, category, tag array, any other fields returned
  - MarketplaceProfileItem: id, orgId, legalName, dba, logoUrl, shortDescription, longDescription, website, hqLocation, yearsInBusiness, employeeCount, contact info fields, any other section fields
  - Bid (if time): id, rfpId, vendor info, submitted fields
- Org-scoping: filtered by boundary automatically; queries cannot cross-boundary
- Pre-fill contributions:
  - `legal_name` ← `MarketplaceProfileItem.legalName`
  - `dba` ← `MarketplaceProfileItem.dba`
  - `logo_url` ← `MarketplaceProfileItem.logoUrl`
  - `short_blurb` ← `MarketplaceProfileItem.shortDescription`
  - `long_description` ← `MarketplaceProfileItem.longDescription`
  - `website` ← `MarketplaceProfileItem.website`
  - `hq_location` ← `MarketplaceProfileItem.hqLocation`
  - `years_in_business` ← `MarketplaceProfileItem.yearsInBusiness`
  - `employee_count` ← `MarketplaceProfileItem.employeeCount`
  - `primary_contact` ← MarketplaceProfileItem contact field (if it exists)
  - RFC4515 filter example: `tag { value: ".eq.<tag-uuid>" }` for tag-based discovery
- Known gaps: date fields (dateCreated, dateLastModified) are DATE type not DATETIME — Phase 28 form renders as date picker; if user edits they must push back as YYYY-MM-DD format
- Write-path: `Pipeline.receive` for all class objects (write happens at ingest time, not post-ingest via SDK)
  </action>
  <verify>
    <automated>test -f .planning/director/platform-data-inventory/gql-class-objects.md && grep -q "MarketplaceProfileItem" .planning/director/platform-data-inventory/gql-class-objects.md && grep -q "RFC4515" .planning/director/platform-data-inventory/gql-class-objects.md</automated>
  </verify>
  <done>gql-class-objects.md created with all four class objects documented, RFC4515 syntax examples included, pre-fill map complete</done>
</task>

<task type="auto">
  <name>Task 2: Audit hydra.Tag API (tag discovery)</name>
  <files>.planning/director/platform-data-inventory/hydra-tag.md</files>
  <read_first>
    - .planning/director/platform-data-inventory/_TEMPLATE.md
    - MEMORY.md § "ZeroBias Tag API" (direct vs. moderated tag creation, tag-name constraints)
    - DECISIONS.md (tag UUIDs: engagement tag `a81cd320-243e-44eb-bdd9-9824019ef3dd`, tag shape validation)
  </read_first>
  <action>
Run live MCP audit of hydra Tag API methods: listTags, searchTags, getTag (all read-side).

**MCP calls:**
1. `mcp__zerobias__zerobias_describe path=/hydra/Tag` to discover method signatures
2. `mcp__zerobias__zerobias_execute service=hydra operation=Tag.listTags` (list all tags accessible to current user/org)
3. `mcp__zerobias__zerobias_execute service=hydra operation=Tag.searchTags` with `{ name: "w3geekery" }` or similar to find W3Geekery-specific tags
4. `mcp__zerobias__zerobias_execute service=hydra operation=Tag.getTag` with `{ id: "a81cd320-243e-44eb-bdd9-9824019ef3dd" }` (W3Geekery engagement tag)

**Document in hydra-tag.md:**
- Signature: listTags(page?, pageSize?) → Tag[], searchTags(input: TagSearchBody) → Tag[], getTag(id: UUID) → Tag
- Sample responses: real tag objects with id, name, ownerId, type, createdAt, updatedAt, description
- Field list: all tag fields present in responses
- Org-scoping: tag scope is "user" or "org" (ownerId indicates scope) — filtered to user's accessible scope automatically
- Pre-fill contributions: NONE (tags are metadata/discovery mechanism, not profile data). However, tags are used in Phase 24 (demo gate filter) and Phase 26 (platform-provider tag)
- Known gaps: tag scope behavior (user vs. org) may need clarification; permission model for listing/creating tags
- Write-path: `Tag.createTag` and `Tag.updateTag` exist but read-only focus for this audit
  </action>
  <verify>
    <automated>test -f .planning/director/platform-data-inventory/hydra-tag.md && grep -q "searchTags" .planning/director/platform-data-inventory/hydra-tag.md && grep -q "a81cd320" .planning/director/platform-data-inventory/hydra-tag.md</automated>
  </verify>
  <done>hydra-tag.md created with all three Tag methods documented, engagement tag UUID confirmed, write-path noted</done>
</task>

<task type="auto">
  <name>Task 3: Audit hydra.Resource API (cross-org discovery)</name>
  <files>.planning/director/platform-data-inventory/hydra-resource.md</files>
  <read_first>
    - .planning/director/platform-data-inventory/_TEMPLATE.md
    - MEMORY.md § "ZeroBias MCP Parameter Patterns" (resource discovery, link-type IDs)
    - .planning/director/bootstrap-w3geekery-engagement.md (Task as Resource pattern)
  </read_first>
  <action>
Run live MCP audit of hydra Resource API methods: listResources, searchResources, getResource (all read-side for this phase).

**MCP calls:**
1. `mcp__zerobias__zerobias_describe path=/hydra/Resource` to discover method signatures
2. `mcp__zerobias__zerobias_execute service=hydra operation=Resource.listResources` with optional filters (exact params from describe)
3. `mcp__zerobias__zerobias_execute service=hydra operation=Resource.searchResources` with `{ name: "..." }` or other search criteria
4. `mcp__zerobias__zerobias_execute service=hydra operation=Resource.getResource` with a known resource UUID (e.g., a Task from the engagement in DECISIONS.md, if available)

**Document in hydra-resource.md:**
- Signature: listResources(page?, pageSize?, filter?) → Resource[], searchResources(body: ResourceSearchBody) → Resource[], getResource(id: UUID) → Resource
- Sample responses: resource objects (Tasks are the primary resource type in current use, but may have others)
- Field list: resourceId, resourceType, name, description, status, owner, createdAt, updatedAt, tags, links (to other resources)
- Org-scoping: resources are org-scoped; filter/search limited to current user's accessible resources
- Pre-fill contributions: NONE (resources are activity/work items, not profile data). However, Tasks are the engagement meta-tracker and future work-item discovery
- Known gaps: resource discovery filters may be limited; link traversal (following links to related resources) may require separate calls
- Write-path: `Resource.tagResource`, `Resource.linkResources`, and `Resource.updateResource` exist but out of audit scope
  </action>
  <verify>
    <automated>test -f .planning/director/platform-data-inventory/hydra-resource.md && grep -q "listResources" .planning/director/platform-data-inventory/hydra-resource.md && grep -q "Task" .planning/director/platform-data-inventory/hydra-resource.md</automated>
  </verify>
  <done>hydra-resource.md created with Resource API methods documented, Task pattern noted</done>
</task>

</tasks>

<verification>
**Post-execution verification:**
1. All three sub-files created with complete template structure (all 7 sections)
2. Sample GQL responses are real W3Geekery class objects (UUIDs, org names, field values match DECISIONS.md)
3. MarketplaceProfileItem field list is exhaustive (legal_name, dba, logo_url, short_blurb, long_description, website, hq_location, years_in_business, employee_count all documented)
4. Pre-fill contributions mapped correctly (all Phase 28 form fields have a source row in pre-fill map)
5. RFC4515 filter syntax documented with real examples
6. RFC4515 filter syntax documented with real examples
7. hydra APIs (Tag, Resource) documented with discovery + write patterns noted
8. Run `bash .planning/phases/25-platform-data-audit/scripts/verify-inventory.sh` — should show ≥9 source files
9. Index file `.planning/director/PLATFORM-DATA-INVENTORY.md` updated with row entries for all 3 GQL/hydra sources

**Manual spot-check (recommended):** Pick 1 GQL query and 1 hydra operation and re-run to verify sample response shape on UAT.
</verification>

<success_criteria>
- [ ] Three sub-files exist: gql-class-objects.md, hydra-tag.md, hydra-resource.md
- [ ] Each file follows template structure (7 sections verified via grep)
- [ ] Sample GQL responses contain real W3Geekery class UUIDs (matches DECISIONS.md)
- [ ] MarketplaceProfileItem fields complete (all Phase 28 form fields present)
- [ ] Pre-fill map populated with {form_field → source → pre_fillable} for all fields
- [ ] RFC4515 filter syntax documented with examples
- [ ] hydra Tag and Resource APIs documented with discovery methods
- [ ] Index file updated with row status = "✅ complete" for all 3 sources
- [ ] `verify-inventory.sh` runs successfully (≥9 sub-files detected)
- [ ] All files committed to git with phase 25 session message
</success_criteria>

<output>
After completion, create `.planning/phases/25-platform-data-audit/25-03-SUMMARY.md` documenting:
- Count and list of 3 GQL/hydra sources audited
- Total class fields documented (Engagement, SmeMartProject, MarketplaceProfileItem, Bid, Task)
- Pre-fill map complete (all Phase 28 form fields have sources OR are in known-unknowns)
- RFC4515 syntax validated with real examples
- Next step: Plan 04 will synthesize pre-fill map and write-path catalog
</output>
