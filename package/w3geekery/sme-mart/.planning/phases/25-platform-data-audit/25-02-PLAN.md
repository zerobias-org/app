---
phase: 25-platform-data-audit
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - .planning/director/platform-data-inventory/whoami.md
  - .planning/director/platform-data-inventory/currentorg.md
  - .planning/director/platform-data-inventory/orgsearch.md
  - .planning/director/platform-data-inventory/usersearch.md
  - .planning/director/platform-data-inventory/boundary.md
  - .planning/director/platform-data-inventory/task.md
autonomous: true
requirements_addressed: [PDA-02]
must_haves:
  truths:
    - "All 6 SDK sources documented with sample responses"
    - "Field lists complete for each source"
    - "Pre-fill map contributions identified"
    - "Known gaps and write-path targets identified"
  artifacts:
    - path: ".planning/director/platform-data-inventory/whoami.md"
      provides: "User identity + admin detection"
      min_sections: 7
    - path: ".planning/director/platform-data-inventory/currentorg.md"
      provides: "Current org metadata"
      min_sections: 7
    - path: ".planning/director/platform-data-inventory/orgsearch.md"
      provides: "Org search + lookup"
      min_sections: 7
    - path: ".planning/director/platform-data-inventory/usersearch.md"
      provides: "User discovery in org"
      min_sections: 7
    - path: ".planning/director/platform-data-inventory/boundary.md"
      provides: "Boundary access + scoping"
      min_sections: 7
    - path: ".planning/director/platform-data-inventory/task.md"
      provides: "Task search + filtering"
      min_sections: 7
  key_links:
    - from: ".planning/director/platform-data-inventory/whoami.md"
      to: ".planning/director/PLATFORM-DATA-INVENTORY.md"
      via: "Sub-file referenced in index, row updated"
      pattern: "Pre-fill contributions mapped to form fields"
    - from: "src/app/core/services/org-switcher.service.ts"
      to: ".planning/director/platform-data-inventory/currentorg.md"
      via: "Existing code patterns extracted for documentation"
      pattern: "Read SDK method usage, document signature"
---

<objective>
Audit the 6 core ZeroBias SDK data sources (whoAmI, getPrincipal, getCurrentOrg, Org search, User search, Boundary, Task) and document each with sample responses, field lists, pre-fill contributions, and write-path targets.

Purpose: Complete the "SDK sources" portion of the mandatory 9-source audit (PDA-02). These sources provide user/org/role/access-control data fundamental to Phase 28 pre-fill.

Output: Six sub-files at `platform-data-inventory/{whoami,currentorg,orgsearch,usersearch,boundary,task}.md`, each following the locked template structure from Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/25-platform-data-audit/25-CONTEXT.md
@.planning/phases/25-platform-data-audit/25-RESEARCH.md
@.planning/director/DECISIONS.md
@.planning/director/bootstrap-w3geekery-engagement.md
@.planning/director/platform-data-inventory/_TEMPLATE.md
@src/app/core/services/org-switcher.service.ts
@src/app/core/services/vendor-profile.service.ts
@.claude/notes/sme-mart-resource-types-summary.md
@CLAUDE.md
</context>

<read_first>
## Critical Context Before Audit

1. **Profile lock required:** Run `~/.claude/scripts/zb-mcp-profile-lock.sh acquire uat-clark@w3geekery phase-25-sdk-audit` before any MCP calls. Release when complete.

2. **W3Geekery test org (live re-run per D-05):** All calls against this org:
   - Org UUID: `cd7105df-523d-5392-9f9a-3f83d3f30107`
   - Clark user UUID: `3da9385a-5d15-4d19-84ab-e1c9ce8d84ed`
   - Org name: "W3Geekery"

3. **MCP discipline (memory § ZeroBias MCP Parameter Patterns):** 
   - Always run `mcp__zerobias__zerobias_describe(path)` FIRST on unfamiliar operations to discover exact param structure
   - Body params nested under param name: `{ newObj: { field: value } }` NOT flat
   - Path/query params at top level: `{ id: "...", pageNumber: 1 }`

4. **Template locked by RESEARCH.md §2:** Every sub-file MUST have these 7 sections in order: frontmatter, Signature, Sample Response, Field List, Pre-fill Map Contributions, Known Gaps/Edge Cases, Write-Path Target.

5. **Sample response policy (D-07):** Real W3Geekery values (UUIDs, org names from actual org). No redaction. Copy-pasteable for debugging.

6. **Verified date:** Use today's date in `verified: YYYY-MM-DD` frontmatter for each sub-file.

7. **Phase 28 form field context (from phase-28-brief.md):** Form renders fields from `company_info` convention: legal_name, dba, logo_url, short_blurb, long_description, primary_contact, website, hq_location, years_in_business, employee_count. Use these as keys in Pre-fill Map Contributions section.
</read_first>

<acceptance_criteria>
- [ ] Each sub-file follows template structure exactly (all 7 sections present)
- [ ] Sample responses are real W3Geekery values (UUIDs match `cd7105df-...` or known DECISIONS.md UUIDs)
- [ ] Field lists complete (every returned field has a row with type, populated status, org-scoping note)
- [ ] Pre-fill contributions section identifies which form fields each source feeds
- [ ] Known gaps section documents nulls, missing fields, limitations per source
- [ ] Write-path targets identified (which SDK setters or Pipeline.receive classes, if any)
- [ ] `verify-inventory.sh` runs and counts ≥6 sub-files in platform-data-inventory/
- [ ] All files committed with session-stamped commit message (see git workflow in MEMORY.md)
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Audit whoAmI + getPrincipal (user identity + admin detection)</name>
  <files>.planning/director/platform-data-inventory/whoami.md</files>
  <read_first>
    - .planning/director/platform-data-inventory/_TEMPLATE.md (locked structure)
    - MEMORY.md § "ZeroBias SDK — Org Selection & dana-org-id" (getWhoAmI vs getPrincipal, principal shape)
    - CLAUDE.md § "SME Mart Admin Detection" (getPrincipal().isAdmin is canonical)
  </read_first>
  <action>
Run live MCP audit of `dana.User.getWhoAmI` and `dana.Org.getPrincipal` on W3Geekery profile. 

**MCP calls to execute:**
1. `mcp__zerobias__zerobias_describe path=/dana/User` to inspect available methods and discover exact signature
2. `mcp__zerobias__zerobias_execute service=dana operation=User.getWhoAmI` (no params)
3. `mcp__zerobias__zerobias_execute service=dana operation=User.getPrincipal` (no params)

**Document in whoami.md:**
- Both methods return user identity + org + admin flag
- getWhoAmI returns User object (name, email, id, org associations)
- getPrincipal returns Principal object with `isAdmin: boolean` flag (CANONICAL for admin detection per CLAUDE.md)
- Admin flag is org-scoped — true if user is admin of currentOrg
- Field list includes: userId, userEmail, userFullName, adminFlag, currentOrgId, currentOrgName, any other fields in response
- Pre-fill contributions: none directly (user data is NOT part of company_info form, which is ORG data not user data)
- Known gaps: no "company primary contact" field — must be looked up separately via User search
- Write-path: none — these are read-only queries
  </action>
  <verify>
    <automated>test -f .planning/director/platform-data-inventory/whoami.md && grep -q "## Signature" .planning/director/platform-data-inventory/whoami.md && grep -q "getPrincipal" .planning/director/platform-data-inventory/whoami.md</automated>
  </verify>
  <done>whoami.md created with both methods documented, field list complete, admin detection mechanism noted</done>
</task>

<task type="auto">
  <name>Task 2: Audit getCurrentOrg (org metadata)</name>
  <files>.planning/director/platform-data-inventory/currentorg.md</files>
  <read_first>
    - .planning/director/platform-data-inventory/_TEMPLATE.md
    - src/app/core/services/org-switcher.service.ts (existing call pattern in code)
    - phase-28-brief.md (form expects: legal_name, dba, logo_url, short_blurb, long_description)
  </read_first>
  <action>
Run live MCP audit of `dana.Org.getCurrentOrg` against W3Geekery profile.

**MCP calls:**
1. `mcp__zerobias__zerobias_describe path=/dana/Org` (discover method signatures)
2. `mcp__zerobias__zerobias_execute service=dana operation=Org.getCurrentOrg` (no params)

**Document in currentorg.md:**
- Returns metadata for currentOrg: name, description, logoUrl, parent, status, createdAt, updatedAt, any other fields
- Field list: orgName (always populated), description (sometimes null), logoUrl (sometimes null), orgId, parent (sometimes null), status, timestamps
- Org-scoped: returns only currentOrg, not settable to other org (for cross-org lookups use Org.get or Org.search)
- Pre-fill contributions:
  - `legal_name` ← `Org.name` (fully pre-fillable, always populated)
  - `dba` ← `Org.description` (partial — null if not set on prod/UAT)
  - `logo_url` ← `Org.logoUrl` (partial — null on UAT, may be set on prod)
- Known gaps: currentOrg has NO short blurb, long description, contact, website, HQ location, years in business, employee count — those come from MarketplaceProfileItem or user input
- Write-path: `platform.Org.update()` or `Pipeline.receive` for Org class if available (verify with zerobias_describe)
  </action>
  <verify>
    <automated>test -f .planning/director/platform-data-inventory/currentorg.md && grep -q "legal_name" .planning/director/platform-data-inventory/currentorg.md && grep -q "Org.name" .planning/director/platform-data-inventory/currentorg.md</automated>
  </verify>
  <done>currentorg.md created with field list and pre-fill contributions mapped to Phase 28 form fields</done>
</task>

<task type="auto">
  <name>Task 3: Audit Org.search + Org.get (org lookup)</name>
  <files>.planning/director/platform-data-inventory/orgsearch.md</files>
  <read_first>
    - .planning/director/platform-data-inventory/_TEMPLATE.md
    - phase-26-brief.md (mentions ZB-as-provider lookup for seeding)
  </read_first>
  <action>
Run live MCP audit of `dana.Org.search` and `dana.Org.get` (org lookup by ID or search criteria).

**MCP calls:**
1. `mcp__zerobias__zerobias_describe path=/dana/Org` (discover search/get signatures)
2. `mcp__zerobias__zerobias_execute service=dana operation=Org.search` with sample params like `{ name: "ZeroBias" }` or leave empty to list accessible orgs
3. For Org.get: `mcp__zerobias__zerobias_execute service=dana operation=Org.get` with `{ id: "<known-org-uuid>" }` (e.g., ZeroBias org or another test org)

**Document in orgsearch.md:**
- Both methods return list/single Org objects with same fields as getCurrentOrg
- Org.search: filtered to orgs accessible to current user (scoping behavior documented)
- Org.get: single org lookup by UUID
- Field list: same as currentorg.md (name, description, logoUrl, etc.)
- Pre-fill contributions: same as currentorg.md (legal_name ← Org.name, etc.), plus useful for Phase 26 ZB-as-provider seeding (can look up ZeroBias org to get metadata)
- Known gaps: search may have limitations (only by name? by type? filtering behavior undocumented?) — note what you discover
- Write-path: none — read-only queries
  </action>
  <verify>
    <automated>test -f .planning/director/platform-data-inventory/orgsearch.md && grep -q "accessible to current user" .planning/director/platform-data-inventory/orgsearch.md</automated>
  </verify>
  <done>orgsearch.md created with search/get methods documented, scoping behavior noted</done>
</task>

<task type="auto">
  <name>Task 4: Audit User.search + User.listUsers (org personnel)</name>
  <files>.planning/director/platform-data-inventory/usersearch.md</files>
  <read_first>
    - .planning/director/platform-data-inventory/_TEMPLATE.md
    - phase-28-brief.md (form has `primary_contact` field)
  </read_first>
  <action>
Run live MCP audit of `dana.User.search` and `dana.User.listUsers` to discover users in the org.

**MCP calls:**
1. `mcp__zerobias__zerobias_describe path=/dana/User` (discover signatures)
2. `mcp__zerobias__zerobias_execute service=dana operation=User.search` with W3Geekery org filter (exact filter syntax TBD by describe)
3. `mcp__zerobias__zerobias_execute service=dana operation=User.listUsers` with W3Geekery org context (if available)

**Document in usersearch.md:**
- Both return list of User objects: userId, email, fullName, status, createdAt, any role/group/status fields
- Field list complete with type and population status
- Org-scoping: filtered to currentOrg automatically or requires explicit filter parameter (document which)
- Pre-fill contributions:
  - `primary_contact` ← User.email or User.fullName (partial — requires manual selection in Phase 28 UI since multiple users may exist)
  - Could also support contact phone/title if those fields exist
- Known gaps: no "primary contact" designation on User object — Phase 28 must let user choose from list, or use first admin, or leave blank
- Write-path: none — read-only
  </action>
  <verify>
    <automated>test -f .planning/director/platform-data-inventory/usersearch.md && grep -q "email" .planning/director/platform-data-inventory/usersearch.md</automated>
  </verify>
  <done>usersearch.md created with user discovery method documented, primary_contact pre-fill strategy noted</done>
</task>

<task type="auto">
  <name>Task 5: Audit Boundary.list + Boundary.get (access control context)</name>
  <files>.planning/director/platform-data-inventory/boundary.md</files>
  <read_first>
    - .planning/director/platform-data-inventory/_TEMPLATE.md
    - .planning/director/bootstrap-w3geekery-engagement.md (Boundary context in walkthrough)
    - DECISIONS.md (W3Geekery Boundary UUID: `c15fb2dc-4f8c-48b5-b27a-707bd516b005`)
  </read_first>
  <action>
Run live MCP audit of `platform.Boundary.list` and `platform.Boundary.get` to understand boundary structure and accessibility.

**MCP calls:**
1. `mcp__zerobias__zerobias_describe path=/platform/Boundary` (discover signatures)
2. `mcp__zerobias__zerobias_execute service=platform operation=Boundary.list` (list boundaries accessible to current user)
3. `mcp__zerobias__zerobias_execute service=platform operation=Boundary.get` with `{ id: "c15fb2dc-4f8c-48b5-b27a-707bd516b005" }` (W3Geekery's SME Marketplace DEV boundary)

**Document in boundary.md:**
- Boundaries represent access scopes within a Boundary (org context + activity type + access level)
- Field list: boundaryId, name, description, parentBoundary, status, parties (list of party IDs with roles), createdAt, updatedAt
- Org-scoping: filtered to boundaries accessible to currentOrg and current user's role
- Pre-fill contributions: NONE directly (no boundary fields in company_info form). However, useful for Phase 27 lazy guard (finds default engagement's boundary) and Phase 31 verification
- Known gaps: internal use only for this audit; boundary selection is not user-facing in v1.4
- Write-path: none — read-only in this phase
  </action>
  <verify>
    <automated>test -f .planning/director/platform-data-inventory/boundary.md && grep -q "boundaryId" .planning/director/platform-data-inventory/boundary.md</automated>
  </verify>
  <done>boundary.md created with boundary structure documented for Phase 27/31 context</done>
</task>

<task type="auto">
  <name>Task 6: Audit Task.list + Task.search (engagement activity tracking)</name>
  <files>.planning/director/platform-data-inventory/task.md</files>
  <read_first>
    - .planning/director/platform-data-inventory/_TEMPLATE.md
    - .planning/director/bootstrap-w3geekery-engagement.md (Task context, meta-tracker pattern)
    - MEMORY.md § "ZeroBias MCP Parameter Patterns" (Task properties, link-type UUIDs)
  </read_first>
  <action>
Run live MCP audit of `platform.Task.list` and `platform.Task.search` to understand task discovery and filtering.

**MCP calls:**
1. `mcp__zerobias__zerobias_describe path=/platform/Task` (discover list/search signatures, filter params)
2. `mcp__zerobias__zerobias_execute service=platform operation=Task.list` (all tasks accessible, may support boundary/org filters)
3. `mcp__zerobias__zerobias_execute service=platform operation=Task.search` with a sample filter like `{ activityId: "..." }` or `{ status: "todo" }` (exact syntax from describe)

**Document in task.md:**
- Task objects: taskId, name, description, status (todo, in-progress, done, etc.), priority (numeric: 1000, 500, 200, 100 per MEMORY.md), assigned, approvers, createdAt, updatedAt, links (to other tasks/resources)
- Field list: all task fields with population status
- Org-scoping: filtered by boundary/org depending on filter params; default behavior TBD by results
- Pre-fill contributions: NONE (tasks are activity tracking, not org profile data). However, task history may inform Phase 28's "engagement activity" or "recent work" sections (deferred)
- Known gaps: task structure is complex; list/search filters may be limited or boundary-specific
- Write-path: `Task.create` + `Task.update` (write methods exist but out of scope for Phase 25)
  </action>
  <verify>
    <automated>test -f .planning/director/platform-data-inventory/task.md && grep -q "taskId" .planning/director/platform-data-inventory/task.md</automated>
  </verify>
  <done>task.md created with task structure documented for Phase 27/31 context and future activity-tracking integration</done>
</task>

</tasks>

<verification>
**Post-execution verification:**
1. All six sub-files created with complete template structure (all 7 sections)
2. Sample responses are real W3Geekery values (UUIDs, org names, emails from actual live calls)
3. Field lists are exhaustive (every field returned has a row)
4. Pre-fill contributions mapped to Phase 28 form fields (legal_name, dba, logo_url, primary_contact, etc.)
5. Known gaps documented (null fields, missing fields, limitations per source)
6. Run `bash .planning/phases/25-platform-data-audit/scripts/verify-inventory.sh` — should show ≥6 source files
7. Index file `.planning/director/PLATFORM-DATA-INVENTORY.md` updated with row entries for all 6 sources (executor updates as part of post-task handoff)

**Manual spot-check (recommended):** Pick 2 random sources and re-run the MCP calls to verify sample response shape still holds on UAT.
</verification>

<success_criteria>
- [ ] Six sub-files exist in `.planning/director/platform-data-inventory/` with names: whoami.md, currentorg.md, orgsearch.md, usersearch.md, boundary.md, task.md
- [ ] Each file follows template structure (7 sections verified via grep)
- [ ] Sample responses contain real W3Geekery UUIDs + field values (verifiable against known test org data)
- [ ] Field lists complete (every returned field documented)
- [ ] Pre-fill contributions section identifies Phase 28 form fields
- [ ] Index file updated with row status = "✅ complete" for each source
- [ ] `verify-inventory.sh` runs successfully
- [ ] All files committed to git with phase 25 session message
</success_criteria>

<output>
After completion, create `.planning/phases/25-platform-data-audit/25-02-SUMMARY.md` documenting:
- Count and list of 6 SDK sources audited
- Total field coverage (how many unique fields across all 6 sources)
- Pre-fill contributions summary (which Phase 28 form fields can be populated from SDK sources)
- Known gaps identified (what field sources could NOT provide)
- Next step: Plan 03 will audit GQL sources (Boundary, Engagement, SmeMartProject, MarketplaceProfileItem)
</output>
