---
phase: 25-platform-data-audit
plan: 04
type: execute
wave: 3
depends_on: [25-02, 25-03]
files_modified:
  - .planning/director/PLATFORM-DATA-INVENTORY.md
  - .planning/director/COMPANY-INFO-CONVENTION-DRAFT.md
autonomous: true
requirements_addressed: [PDA-03, PDA-04]
must_haves:
  truths:
    - "Pre-fill map covers all Phase 28 form fields"
    - "Known-unknown list identifies unpopulatable fields"
    - "Write-path map documents where Phase 28 saves data"
    - "Draft company_info convention produced for Phase 26 ratification"
  artifacts:
    - path: ".planning/director/PLATFORM-DATA-INVENTORY.md"
      provides: "Complete index with populated pre-fill map and known-unknowns"
      min_lines: 150
    - path: ".planning/director/COMPANY-INFO-CONVENTION-DRAFT.md"
      provides: "Draft YAML convention for provider/buyer org profiles"
      min_lines: 80
  key_links:
    - from: ".planning/director/PLATFORM-DATA-INVENTORY.md"
      to: ".planning/director/phase-28-brief.md"
      via: "Phase 28 form mount reads pre-fill map to populate fields"
      pattern: "Every form field has a pre-fill row"
    - from: ".planning/director/COMPANY-INFO-CONVENTION-DRAFT.md"
      to: ".planning/director/phase-26-brief.md"
      via: "Phase 26 ratifies the convention"
      pattern: "Canonical shape for all providers and buyers"
---

<objective>
Synthesize audit data from Plans 02–03 into two deliverables: (1) complete pre-fill map in the index file covering every Phase 28 form field, (2) draft `company_info` convention YAML for Phase 26 to ratify.

Purpose: Bridge the audit phase (data gathering) and the downstream phases (Phase 26 ratification, Phase 28 implementation).

Output: Updated index file with pre-fill + known-unknowns complete, plus draft convention document for Phase 26.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/25-platform-data-audit/25-RESEARCH.md
@.planning/director/DECISIONS.md
@.planning/director/COMPANY-INFO-CONVENTION-DRAFT.md (target file)
@.planning/director/phase-28-brief.md
@.planning/director/phase-26-brief.md
@.planning/director/PLATFORM-DATA-INVENTORY.md (from Plans 02–03)
@.planning/director/platform-data-inventory/*.md (all sub-files from Plans 02–03)
</context>

<read_first>
## Synthesis Context

1. **Phase 28 form field enumeration (from phase-28-brief.md):**
   - legal_name (required)
   - dba (optional)
   - logo_url (optional)
   - short_blurb (optional, max 500 chars)
   - long_description (optional)
   - primary_contact (optional)
   - website (optional)
   - hq_location (optional)
   - years_in_business (optional)
   - employee_count (optional)
   
   Use this list as the definitive form-field set. If the brief enumerates fields differently, use the brief.

2. **Pre-fill map locked columns (D-08, D-12, RESEARCH.md §8):**
   - form_field: Phase 28 form field name
   - source_call: SDK/GQL/hydra operation
   - platform_field_path: Exact field in response
   - pre_fillable: yes / no / partial (with rationale)
   - save_target: Which Platform class + field, or hydra operation, to write back
   - notes: Why partial/no, edge cases, org-scoping behavior

3. **Known-unknown classification (D-03):**
   - Flat list with free-text "why" note per field
   - Examples:
     - `primary_contact`: No primary-contact designation on User object. Phase 28 may auto-select first admin or leave blank pending user selection.
     - `employee_count`: No field in Org or MarketplaceProfileItem. Requires user input.
     - `years_in_business`: No field in Org schema. Requires user input or LLM enrichment.

4. **company_info convention shape (D-01, RESEARCH.md §9):**
   - YAML document defining canonical field set for provider/buyer profiles
   - Used by: Phase 26 (seeding ZB-as-provider), Phase 28 (form rendering), Phase 31 (verification)
   - Proposed in Phase 25; ratified in Phase 26; implemented in Phase 28+

5. **Write-path discovery (D-12):**
   - For each form field, identify WHERE Phase 28 writes the saved value
   - Examples:
     - legal_name → `Pipeline.receive MarketplaceProfileItem.legalName`
     - engagementTag → `Pipeline.receive Engagement.engagementTag`
     - If no write-path exists, note "read-only" or "requires manual import"
</read_first>

<acceptance_criteria>
- [ ] Pre-fill map table in index file populated with ONE row per Phase 28 form field
- [ ] All rows have: form_field, source_call, platform_field_path, pre_fillable status, save_target, notes
- [ ] Known-unknowns list complete (fields not pre-fillable, with "why" notes per D-03)
- [ ] Known-unknowns count ≥ 1 (at least some fields require user input; none of the 10 fields are 100% auto-fillable on UAT)
- [ ] COMPANY-INFO-CONVENTION-DRAFT.md created with YAML structure
- [ ] Convention covers all Phase 28 form fields + any additional fields from audited MarketplaceProfileItem
- [ ] Convention includes type, required flag, description, source, and save_target per field
- [ ] Draft convention notes that Phase 26 will ratify and Phase 28 will implement
- [ ] `verify-inventory.sh` runs successfully
- [ ] All files committed to git
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Synthesize pre-fill map and update index file</name>
  <files>.planning/director/PLATFORM-DATA-INVENTORY.md</files>
  <read_first>
    - .planning/director/PLATFORM-DATA-INVENTORY.md (current index skeleton)
    - .planning/director/platform-data-inventory/whoami.md through gql-class-objects.md (all 9 sub-files from Plans 02–03)
    - .planning/director/phase-28-brief.md (form fields)
    - RESEARCH.md § "Pre-Fill Map Column Schema (D-08, D-12)" (locked columns)
  </read_first>
  <action>
Update `.planning/director/PLATFORM-DATA-INVENTORY.md` with:

1. **Complete Pre-fill Map table** (replace the placeholder row):
   - Extract every form field from phase-28-brief.md (legal_name, dba, logo_url, short_blurb, long_description, primary_contact, website, hq_location, years_in_business, employee_count)
   - For each field, read through Plans 02–03 sub-files and find:
     - Which source provides this field (SDK whoAmI/getPrincipal/getCurrentOrg/Org.search/User.search/boundary/task? GQL MarketplaceProfileItem? hydra tag/resource?)
     - What is the exact field path in that source (e.g., Org.name, MarketplaceProfileItem.legalName)
     - Is it always populated, sometimes null, or never exists?
     - Where does Phase 28 write this field back when user saves? (e.g., Pipeline.receive MarketplaceProfileItem, Org.update if available, hydra.Resource.updateResource)
   - Row example:
     ```
     | legal_name | dana.Org.getCurrentOrg + graphql.MarketplaceProfileItem | Org.name + MarketplaceProfileItem.legalName | yes (Org.name always populated, MarketplaceProfileItem.legalName from audit) | Pipeline.receive MarketplaceProfileItem.legalName | Org provides the raw org name; MarketplaceProfileItem is the business-facing profile. Phase 28 saves to MarketplaceProfileItem for consistency. |
     ```

2. **Known-Unknowns section** (replace placeholder):
   - Identify fields in Phase 28 form that NO audit source provided:
     - `years_in_business`: No field in Org, MarketplaceProfileItem, SDK sources. Requires user input.
     - `employee_count`: No field. Requires user input or bucket selection (1-10, 10-50, 50-500, 500+?). Possible LLM enrichment later.
     - `website`: Check if MarketplaceProfileItem has this field. If not, known-unknown.
     - `hq_location`: Check if MarketplaceProfileItem has city/country/address. If not, known-unknown.
     - `primary_contact`: No primary-contact designation on User. Must be selected from list or left blank.
   - Format: Bullet list with "why" note per field (per D-03)
     ```
     - `years_in_business`: No field in Org schema or MarketplaceProfileItem. Requires user input or future LLM enrichment from public data.
     - `employee_count`: No field. Requires user input, possibly as bucket selection (1-10, 10-50, etc.) for GDPR compliance.
     ```

3. **Update Audit Coverage table:**
   - All 9 mandatory sources should show "✅ complete" status
   - Count opportunistic expansion sources added (if any; e.g., MarketplaceProfileItem discovered as separate source, or Org.listMembers, or portal.Product)

4. **Update verified date** at top of file to today's date

5. **Add internal notes section** documenting:
   - Spot-check results (if manual verification run post-Plan 03)
   - Any discrepancies between pre-fill plan and actual audit findings
   - Aggressive expansion sources added beyond the 9 mandatory
  </action>
  <verify>
    <automated>grep -c "^\| " .planning/director/PLATFORM-DATA-INVENTORY.md | awk '{if ($1 >= 10) exit 0; else exit 1}' && grep -q "## Known Unknowns" .planning/director/PLATFORM-DATA-INVENTORY.md && grep -q "years_in_business" .planning/director/PLATFORM-DATA-INVENTORY.md</automated>
  </verify>
  <done>Index file updated with complete pre-fill map (≥10 rows for form fields), known-unknowns list populated with "why" notes per D-03</done>
</task>

<task type="auto">
  <name>Task 2: Create draft company_info convention YAML</name>
  <files>.planning/director/COMPANY-INFO-CONVENTION-DRAFT.md</files>
  <read_first>
    - RESEARCH.md § "Draft company_info Convention Shape" (proposed YAML structure, partial example)
    - .planning/director/platform-data-inventory/gql-class-objects.md (MarketplaceProfileItem field list)
    - .planning/director/platform-data-inventory/currentorg.md (Org fields)
    - .planning/director/phase-28-brief.md (form fields)
    - .planning/director/phase-26-brief.md (convention use in seeding)
  </read_first>
  <action>
Create file `.planning/director/COMPANY-INFO-CONVENTION-DRAFT.md` with the following structure (locked shape per RESEARCH.md §9, modified by audit findings):

```markdown
---
status: draft
phase_created: 25
phase_ratified_target: 26
verified: YYYY-MM-DD
---

# company_info Convention

## Purpose

Canonical shape for all provider and buyer company profiles in SME Mart. Used by:
- Phase 26: Seeding ZeroBias as the first marketplace provider
- Phase 28: Collecting buyer company profile on onboarding
- Phase 31: Verification and profile completeness checks

## Convention

```yaml
CompanyInfo:
  description: Canonical shape for marketplace provider/buyer company profiles
  properties:
    
    # Legal/Corporate Identity
    legal_name:
      type: string
      required: true
      description: Registered legal business name
      source:
        - da.Org.name (org identity layer)
        - MarketplaceProfileItem.legalName (marketplace profile layer)
      save_target: Pipeline.receive MarketplaceProfileItem.legalName
      
    dba:
      type: string|null
      required: false
      description: Doing Business As name (if different from legal_name)
      source:
        - Org.description (partial; may be misused for blurbs)
        - MarketplaceProfileItem.dba (if it exists in audit)
      save_target: Pipeline.receive MarketplaceProfileItem.dba
      
    # Branding
    logo_url:
      type: string|null
      required: false
      description: Logo image URL
      source:
        - Org.logoUrl (may be null on UAT)
        - MarketplaceProfileItem.logoUrl (if audited)
      save_target: Pipeline.receive MarketplaceProfileItem.logoUrl
      
    short_blurb:
      type: string|null
      required: false
      max_length: 500
      description: One-liner company tagline
      source:
        - MarketplaceProfileItem.shortDescription (from audit)
      save_target: Pipeline.receive MarketplaceProfileItem.shortDescription
      
    long_description:
      type: string|null
      required: false
      description: Multi-paragraph company overview
      source:
        - MarketplaceProfileItem.longDescription (from audit)
      save_target: Pipeline.receive MarketplaceProfileItem.longDescription
      
    # Contact & Location
    primary_contact:
      type: object|null
      required: false
      description: Primary contact person (email + name)
      properties:
        name:
          type: string
          description: Contact person full name
        email:
          type: string
          description: Contact email
      source:
        - dana.User.search (requires manual selection from org users; no primary-contact designator)
      save_target: Pipeline.receive (field path TBD; may require new MarketplaceProfileItem fields)
      
    website:
      type: string|null
      required: false
      description: Company website URL
      source:
        - MarketplaceProfileItem.website (from audit; if exists)
      save_target: Pipeline.receive MarketplaceProfileItem.website
      
    hq_location:
      type: object|null
      required: false
      description: Headquarters location
      properties:
        city:
          type: string
        state_or_region:
          type: string
        country:
          type: string
      source:
        - MarketplaceProfileItem fields (if audited; may need to check city/state/country separately)
      save_target: Pipeline.receive (field path TBD; may require new MarketplaceProfileItem sub-object)
      
    # Business Profile
    years_in_business:
      type: number|null
      required: false
      description: Years the company has been in operation
      source:
        - (KNOWN UNKNOWN — not in Org, MarketplaceProfileItem, or SDK)
      save_target: Pipeline.receive (new field needed if we want to store this)
      
    employee_count:
      type: string|null
      required: false
      description: Approximate employee count (e.g., 1-10, 10-50, 50-500, 500+)
      source:
        - (KNOWN UNKNOWN — not in Org or MarketplaceProfileItem)
        - Future LLM enrichment from public data (deferred director brief)
      save_target: Pipeline.receive (new field needed if we want to store this)
```

## Notes

- Phase 25 produced this draft by auditing ZB platform SDK, GQL, and hydra APIs against W3Geekery test org.
- Fields marked KNOWN UNKNOWN require user input or future enrichment (e.g., LLM-assisted lookups from company public data).
- Phase 26 (Seed Provider) will ratify this convention and use it to seed ZeroBias as a marketplace provider.
- Phase 28 (Company Profile Form) will render every field in this convention as a form field (some pre-filled, some requiring user input).
- Future extensibility: New fields can be added to convention in later phases if needed (e.g., compliance certifications, pricing tiers, service areas).
```

Fill in `verified: YYYY-MM-DD` with today's date. Leave `phase_ratified_target: 26` as-is — Phase 26 will ratify and update status to "ratified" per D-01.
  </action>
  <verify>
    <automated>test -f .planning/director/COMPANY-INFO-CONVENTION-DRAFT.md && grep -q "legal_name:" .planning/director/COMPANY-INFO-CONVENTION-DRAFT.md && grep -q "phase_ratified_target: 26" .planning/director/COMPANY-INFO-CONVENTION-DRAFT.md</automated>
  </verify>
  <done>Convention draft created with all Phase 28 form fields + audit-discovered MarketplaceProfileItem fields. Known unknowns documented. Marked as Phase 26 ratification target.</done>
</task>

</tasks>

<verification>
**Post-execution verification:**
1. Index file has ≥10 rows in pre-fill map (one per form field)
2. All rows have: form_field, source_call, platform_field_path, pre_fillable, save_target, notes (5 required columns per D-12)
3. Known-unknowns list has ≥1 field (not everything is pre-fillable from platform on UAT)
4. Convention draft covers all 10 form fields + any extras discovered in audit
5. Convention is YAML-formatted and ready for Phase 26 ratification
6. Run `verify-inventory.sh` — should still show ≥9 sub-files
7. Both files committed to git

**Manual review (recommended):** Phase 28 brief reviewer should scan convention fields and confirm they match their expected form structure.
</verification>

<success_criteria>
- [ ] Index file `PLATFORM-DATA-INVENTORY.md` has pre-fill map with ≥10 rows (one per form field)
- [ ] All pre-fill rows have 6 columns: form_field, source_call, platform_field_path, pre_fillable, save_target, notes
- [ ] Known-unknowns section populated with ≥1 field
- [ ] Convention draft file exists at `.planning/director/COMPANY-INFO-CONVENTION-DRAFT.md`
- [ ] Convention is valid YAML with all Phase 28 form fields as YAML properties
- [ ] Convention marked as draft, ratification target Phase 26
- [ ] `verify-inventory.sh` runs successfully
- [ ] All files committed to git with phase 25 session message
</success_criteria>

<output>
After completion, create `.planning/phases/25-platform-data-audit/25-04-SUMMARY.md` documenting:
- Pre-fill map complete: N fields with sources, M fields as known-unknowns
- Convention shape: N fields total, list of known-unknowns
- Handoff to Phase 26: Convention needs ratification before Phase 28 can implement
- Next step: Plan 05 will run pipeline health check and commit env-file fix
</output>
