---
phase: 04-wave-3-standalone-entities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/core/services/service-offerings.service.ts
  - src/app/core/services/service-offerings.service.spec.ts
  - src/app/core/services/reviews.service.ts
  - src/app/core/services/reviews.service.spec.ts
autonomous: true
requirements: [MIG-16, MIG-17, MIG-18, MIG-19]

must_haves:
  truths:
    - ServiceOfferingsService.createService() pushes entities to Pipeline and returns optimistically
    - ServiceOfferingsService.listServices() and getServicesByProvider() query ServiceOffering via GraphQL (no Neon fallback)
    - ReviewsService.createReview() pushes entities to Pipeline and returns optimistically
    - ReviewsService.listReviewsByProvider() and listAdminReviews() query Review via GraphQL (no Neon fallback)
    - All service methods apply correct field mapping (snake_case Neon → camelCase GQL)
  artifacts:
    - path: src/app/core/services/service-offerings.service.ts
      provides: ServiceOfferingsService migrated to PipelineWriteService + GraphqlReadService
      imports: [PipelineWriteService, GraphqlReadService, mapGqlToNeon, SERVICE_OFFERING_FIELD_MAPPING]
    - path: src/app/core/services/reviews.service.ts
      provides: ReviewsService migrated to PipelineWriteService + GraphqlReadService
      imports: [PipelineWriteService, GraphqlReadService, mapGqlToNeon, REVIEW_FIELD_MAPPING]
    - path: src/app/core/services/service-offerings.service.spec.ts
      provides: Full test suite (create, read, list, filter, delete)
      min_tests: 5
    - path: src/app/core/services/reviews.service.spec.ts
      provides: Full test suite (create, approve, reject, list, admin list)
      min_tests: 6
  key_links:
    - from: ServiceOfferingsService.createService()
      to: PipelineWriteService.pushEntity('ServiceOffering', ...)
      via: Fire-and-forget pattern (return optimistically)
    - from: ServiceOfferingsService.listServices()
      to: GraphqlReadService.query('ServiceOffering', ...)
      via: RFC4515 filters (isActive: '.eq.true')
    - from: ReviewsService.createReview()
      to: PipelineWriteService.pushEntity('Review', ...)
      via: Fire-and-forget with optimistic return
    - from: ReviewsService.listReviewsByProvider()
      to: GraphqlReadService.query('Review', ...)
      via: RFC4515 filters (providerId: '.eq.{id}', approved filter optional)
---

<objective>
Migrate ServiceOffering (catalog) and Review entities from SmeMartDbService to PipelineWriteService + GraphqlReadService, completing Wave 3 of the AuditgraphDB migration. Both services are standalone with no cross-entity dependencies—apply the proven Phase 2-3 swap pattern mechanically.

**Purpose:** Finish migration of all 8 original SME Mart entities. No remaining Neon reads/writes for migrated types.

**Output:**
- ServiceOfferingsService: PipelineWriteService for create/update/delete, GraphqlReadService for all queries
- ReviewsService: PipelineWriteService for create/approve/reject, GraphqlReadService for list operations
- Full test suites for both (≥80% coverage, using fakePipelineWriteService + fakeGraphqlReadService mocks)
- All methods apply correct field mappings (SERVICE_OFFERING_FIELD_MAPPING, REVIEW_FIELD_MAPPING)
</objective>

<execution_context>
@/Users/cstacer/.claude/get-shit-done/workflows/execute-plan.md

## Prior Phase Reference

Phase 2-3 migration patterns (proven and working):
- `src/app/core/services/engagements.service.ts` (Phase 2)
- `src/app/core/services/notes.service.ts` (Phase 3)
- `src/app/core/services/bids.service.ts` (Phase 2)

Test helpers and infrastructure (Phase 1, reused here):
- `src/app/test-helpers/angular.ts` — fakePipelineWriteService(), fakeGraphqlReadService()
- `src/app/core/field-mappings.ts` — SERVICE_OFFERING_FIELD_MAPPING, REVIEW_FIELD_MAPPING, mapNeonToGql, mapGqlToNeon
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-wave-3-standalone-entities/04-CONTEXT.md
@.planning/phases/04-wave-3-standalone-entities/04-RESEARCH.md

## Current Code State

**ServiceOfferingsService:** Currently injects SmeMartDbService for all CRUD. Field mappings (SERVICE_OFFERING_FIELD_MAPPING) and GQL types (GqlServiceOfferingResponse) exist from Phase 1.

**ReviewsService:** Currently injects SmeMartDbService for all CRUD + admin views (v_admin_reviews VIEW). Field mappings (REVIEW_FIELD_MAPPING) and GQL types (GqlReviewResponse) exist from Phase 1.

**Test status:** No existing service spec files for either service (Wave 0 gaps per research). Both services need full test suites using mocks.

## Key Constraints

- Follow Phase 2-3 pattern exactly (proven, tested, low-risk)
- Field mappings already exist—no new mapping logic needed
- GQL types already exist—use GqlServiceOfferingResponse and GqlReviewResponse
- Fire-and-forget Pipeline pushes (return optimistically)
- RFC4515 filters for all queries (`.eq.`, `.ilike.*`, etc.)
- No new functionality; just swap backend data sources
- Review → Task approval workflow is Phase 7 deferred—don't implement it
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ServiceOfferingsService from SmeMartDbService to PipelineWriteService + GraphqlReadService</name>
  <files>src/app/core/services/service-offerings.service.ts</files>
  <read_first>
    - src/app/core/services/service-offerings.service.ts (current implementation)
    - src/app/core/services/engagements.service.ts (Phase 2 reference pattern)
    - src/app/core/field-mappings.ts (SERVICE_OFFERING_FIELD_MAPPING)
    - src/app/core/gql-types/service-offering.types.ts (GqlServiceOfferingResponse)
  </read_first>
  <action>
Replace SmeMartDbService injection with PipelineWriteService + GraphqlReadService. Rewrite all methods:

**createService():** Build GQL object with camelCase field names (title, description, providerId, category, pricingType, price, deliveryTime, includes, requirements, isActive, createdAt, updatedAt). Generate UUID via crypto.randomUUID(). Push to Pipeline with `pipelineWrite.pushEntity('ServiceOffering', gqlData)` (fire-and-forget). Return optimistically by mapping GQL back to Neon shape using mapGqlToNeon(gqlData, SERVICE_OFFERING_FIELD_MAPPING.gqlToNeon).

**updateService():** Build GQL object with updated fields. Fire-and-forget push. Return optimistic result (original entity with updated fields).

**deleteService():** Call pipelineWrite.deleteEntity('ServiceOffering', serviceId) (fire-and-forget).

**listServices():** Query via GraphQL with filters { isActive: '.eq.true' }. Request fields: id, providerId, title, description, category, subcategory, pricingType, price, deliveryTime, includes, requirements, isActive, createdAt, updatedAt. Transform results with mapGqlToNeon(). Return PagedResults with pagination.

**getServicesByProvider():** Query via GraphQL with filter { providerId: '.eq.{providerId}' }. Transform results and return as array (no pagination).

**Field mapping:** Use existing SERVICE_OFFERING_FIELD_MAPPING constant for Neon ↔ GQL transformations. Handle nullable fields (price, deliveryTime, includes, requirements, subcategory) explicitly with `?? null`.

**Error handling:** Add try-catch with console.error for Pipeline failures (non-blocking since optimistic updates are returned first).
  </action>
  <verify>
    <automated>
      npm test -- src/app/core/services/service-offerings.service.spec.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - ServiceOfferingsService no longer imports SmeMartDbService
    - createService() returns immediately with generated ID; Pipeline push is fire-and-forget
    - listServices() queries 'ServiceOffering' via GraphQL with isActive filter
    - getServicesByProvider() queries 'ServiceOffering' via GraphQL with providerId filter
    - updateService() and deleteService() push to Pipeline (fire-and-forget)
    - All methods apply field mappings via mapGqlToNeon()
    - No Neon reads anywhere in the service
    - Field mapping roundtrip test passes (existing from Phase 1 or updated if needed)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate ReviewsService from SmeMartDbService to PipelineWriteService + GraphqlReadService</name>
  <files>src/app/core/services/reviews.service.ts</files>
  <read_first>
    - src/app/core/services/reviews.service.ts (current implementation)
    - src/app/core/services/notes.service.ts (Phase 3 reference pattern)
    - src/app/core/field-mappings.ts (REVIEW_FIELD_MAPPING)
    - src/app/core/gql-types/review.types.ts (GqlReviewResponse)
  </read_first>
  <action>
Replace SmeMartDbService injection with PipelineWriteService + GraphqlReadService. Rewrite all methods:

**createReview():** Build GQL object with camelCase field names (id, providerId, reviewerZerobiasUserId, engagementId, rating, reviewText, approved, approvedAt, approvedBy, createdAt, updatedAt). Generate UUID. Push to Pipeline with fire-and-forget. Return optimistically (approved defaults to false, approvedAt/approvedBy are null).

**approveReview():** Build partial GQL object with approved=true, approvedAt={now}, approvedBy={approverUserId}. Fire-and-forget push. Return optimistic result.

**rejectReview():** Similar to approveReview() but set approved=false if that's your logic, or delete the review entirely via pipelineWrite.deleteEntity() — check current code for intended behavior.

**listReviewsByProvider():** Query via GraphQL with filters { providerId: '.eq.{providerId}', ...(approvedOnly && { approved: '.eq.true' }) }. Request all Review fields. Transform and return as array.

**listAdminReviews():** Replace v_admin_reviews VIEW query with GraphQL query on Review class. Request fields: id, providerId, reviewerZerobiasUserId, engagementId, rating, reviewText, approved, approvedAt, approvedBy, createdAt, updatedAt. Support pagination via pageNumber/pageSize.

**listPendingReviews():** Query with filter { approved: '.eq.false' }. Return PagedResults with pagination.

**Field mapping:** Use existing REVIEW_FIELD_MAPPING constant. Handle nullable fields (engagementId/request_id, reviewText, approvedAt, approvedBy) explicitly with `?? null`.

**Note on approval flow:** The Review schema has a `linkTo: Task` field for approval workflow, but that's Phase 7 deferred (per CONTEXT.md). Don't implement Task linking now. Just migrate the existing approval fields (approved, approved_at, approved_by).
  </action>
  <verify>
    <automated>
      npm test -- src/app/core/services/reviews.service.spec.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - ReviewsService no longer imports SmeMartDbService
    - createReview() returns immediately with generated ID; Pipeline push is fire-and-forget
    - approveReview() and rejectReview() push to Pipeline (fire-and-forget)
    - listReviewsByProvider() queries 'Review' via GraphQL with providerId and optional approved filter
    - listAdminReviews() queries 'Review' via GraphQL (no v_admin_reviews VIEW)
    - listPendingReviews() queries with approved=false filter
    - All methods apply field mappings via mapGqlToNeon()
    - No Neon reads anywhere in the service (no VIEW fallback)
    - No Task linking implementation (deferred to Phase 7)
    - Field mapping roundtrip test passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Write comprehensive unit test suites for both services (>= 5 tests each)</name>
  <files>
    - src/app/core/services/service-offerings.service.spec.ts
    - src/app/core/services/reviews.service.spec.ts
  </files>
  <read_first>
    - src/app/test-helpers/angular.ts (fakePipelineWriteService, fakeGraphqlReadService)
    - src/app/core/services/engagements.service.spec.ts (Phase 2 reference test pattern)
    - src/app/core/services/notes.service.spec.ts (Phase 3 reference test pattern)
  </read_first>
  <action>
Create service.spec.ts files using Angular TestBed + Vitest. Use fakePipelineWriteService() and fakeGraphqlReadService() mocks from test-helpers.

**ServiceOfferingsService.spec.ts tests (≥ 5):**
1. createService() returns entity with generated UUID and calls pipelineWrite.pushEntity()
2. listServices() queries GraphQL with isActive filter and returns PagedResults
3. getServicesByProvider() queries GraphQL with providerId filter and returns array
4. updateService() pushes to Pipeline and returns optimistically
5. deleteService() calls pipelineWrite.deleteEntity()
6. listServices() applies field mapping (Neon shape in results, not GQL camelCase)

**ReviewsService.spec.ts tests (≥ 6):**
1. createReview() returns entity with generated UUID and calls pipelineWrite.pushEntity()
2. listReviewsByProvider() queries GraphQL with providerId filter and optional approved filter
3. listAdminReviews() queries GraphQL (not v_admin_reviews VIEW) with pagination
4. listPendingReviews() queries with approved=false filter
5. approveReview() pushes updates to Pipeline (approved=true, approvedAt, approvedBy)
6. rejectReview() pushes to Pipeline or deletes (verify current behavior)
7. All results use mapGqlToNeon() to return Neon shape (not GQL)

**Pattern for both:**
- Setup TestBed with mocks: `{ provide: PipelineWriteService, useValue: fakePipelineWriteService() }`
- Inject service and mocks: `service = TestBed.inject(ServiceOfferingsService)`
- Verify Pipeline/GQL calls: `expect(pipelineWrite.pushEntity).toHaveBeenCalledWith('ServiceOffering', expect.any(Object))`
- Verify results are in Neon shape: `expect(result.provider_id).toBe(...)` not `expect(result.providerId).toBe(...)`
- Use existing SERVICE_OFFERING_GQL_FIXTURE and REVIEW_GQL_FIXTURE for mock responses (from Phase 1 if available, or create them)

**Coverage target:** ≥ 80% branch coverage (use `npm test -- --coverage` to verify).
  </action>
  <verify>
    <automated>
      npm test -- src/app/core/services/service-offerings.service.spec.ts --reporter=verbose && npm test -- src/app/core/services/reviews.service.spec.ts --reporter=verbose
    </automated>
  </verify>
  <acceptance_criteria>
    - service-offerings.service.spec.ts has ≥ 5 passing tests
    - reviews.service.spec.ts has ≥ 6 passing tests
    - All tests use fakePipelineWriteService() + fakeGraphqlReadService() mocks (no real API calls)
    - Tests verify field mapping (results in Neon shape, not GQL camelCase)
    - Tests verify Pipeline calls are made with correct entity types
    - Tests verify GQL queries use correct filters and field selections
    - Coverage: ≥ 80% branch coverage for both services
    - `npm test` (full suite) passes without new errors
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Grep: No SmeMartDbService imports in migrated files:**
   ```bash
   grep -n "SmeMartDbService" src/app/core/services/service-offerings.service.ts src/app/core/services/reviews.service.ts
   ```
   Expected: No matches (0 occurrences)

2. **Grep: PipelineWriteService imported in both files:**
   ```bash
   grep -n "PipelineWriteService" src/app/core/services/service-offerings.service.ts src/app/core/services/reviews.service.ts
   ```
   Expected: Both files import PipelineWriteService

3. **Grep: GraphqlReadService imported in both files:**
   ```bash
   grep -n "GraphqlReadService" src/app/core/services/service-offerings.service.ts src/app/core/services/reviews.service.ts
   ```
   Expected: Both files import GraphqlReadService

4. **Test suite passes:**
   ```bash
   npm test
   ```
   Expected: All tests green, no failures

5. **No regressions:** Components using these services (ServiceCatalogComponent, MyProfileReviewsComponent, AdminReviewsComponent if they exist) should still load without errors. No component changes needed (domain service public API is unchanged).
</verification>

<success_criteria>
Phase 4 is complete when:

1. **ServiceOfferingsService fully migrated:** createService, updateService, deleteService push to Pipeline (optimistic); listServices, getServicesByProvider query GraphQL; no SmeMartDbService injection
2. **ReviewsService fully migrated:** createReview, approveReview, rejectReview push to Pipeline; listReviewsByProvider, listAdminReviews, listPendingReviews query GraphQL; v_admin_reviews VIEW replaced with GQL query; no SmeMartDbService injection
3. **Field mappings applied correctly:** All service methods use SERVICE_OFFERING_FIELD_MAPPING and REVIEW_FIELD_MAPPING for Neon ↔ GQL transformation
4. **Full test coverage:** service-offerings.service.spec.ts (≥ 5 tests) and reviews.service.spec.ts (≥ 6 tests) all passing; ≥ 80% branch coverage
5. **All 4 requirements addressed:**
   - MIG-16: ServiceOfferingsService writes via Pipeline ✓
   - MIG-17: ServiceOfferingsService reads via GraphQL ✓
   - MIG-18: ReviewsService writes via Pipeline ✓
   - MIG-19: ReviewsService reads via GraphQL ✓
6. **No regressions:** `npm test` (full suite) passes
</success_criteria>

<output>
After successful execution, create:
`.planning/phases/04-wave-3-standalone-entities/04-01-SUMMARY.md`

Summary should include:
- Tasks completed (all 3)
- Files migrated (service-offerings.service.ts, reviews.service.ts)
- Tests added (service-offerings.service.spec.ts, reviews.service.spec.ts)
- Requirements addressed (MIG-16, MIG-17, MIG-18, MIG-19)
- Test results (count, coverage %)
- Any issues encountered and resolved
- Commits created (one per task or combined)
</output>
