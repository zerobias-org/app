---
id: "022"
priority: high
scope: platform infra + dev environment
effort: small
found: 2026-05-05
status: open
promoted_to: null
---

# Provision Geekery as member of ZeroBias.Org and validate org-switcher

Brian's named immediate unblock: coordinate with Kevin McCarthy + Chris to provision Geekery as a `member` user/role in ZeroBias.Org on the relevant platform environment(s) (CI? UAT? — see #024). Once provisioned, validate the org-switcher works end-to-end between Geekery's primary org and ZeroBias.Org. If org-switching to .Org doesn't work, escalate to Kevin/team to make it work — Brian called it a "default scenario" that must work.

Without this, Clark cannot validate any UI/dev work for the .Org engagement model.

**Why now:** Brian explicitly named this as "the immediate unblock" — "the big thing is, get with Kevin and Chris and say that you need Geekery to be a member of ZeroBias.Org. That way, structurally, everything kind of snaps in."

**Blocked by:** #024 (need to know which envs ZB.Org exists on first).

**See:** `.planning/notes/meetings/2026-05-05-marketplace.md`
