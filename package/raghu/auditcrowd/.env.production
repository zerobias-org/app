# Committed, no secrets. Loaded by `next build` for the static-export deliverable.
# The same build is promoted to uat / qa / prod — the client resolves the API
# host from location.host at runtime, so no per-env API hostname is baked in.
NEXT_PUBLIC_IS_LOCAL_DEV=false
NEXT_PUBLIC_BASE_PATH=/auditcrowd
